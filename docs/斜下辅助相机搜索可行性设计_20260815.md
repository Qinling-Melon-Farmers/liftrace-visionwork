# 斜下辅助相机搜索可行性设计

日期：2026-08-15

分支：`feat/oblique-camera-search-feasibility`

状态：**隔离视觉原型、OpenCV 蓝环粗发现和主动裁路 A/B 已完成首轮；固定坐标下出现一次
显著收益和一次规划退化，稳定性尚未通过。全随机全靶标、多 seed、单 YOLO 双相机调度与
硬件选型尚未完成。**

## 1. 结论和业务边界

本原型用于回答一个有限问题：在不改动现有下视权威链的前提下，斜下相机能否更早发现
靶标，并给出足以让导航抵近的粗地图点。

- 下视相机、六分类 + 圆环/红十字/H 几何、stable ID、投递对准和释放证据均保持不变；
- 辅助相机只输出粗检测、方位、粗地图点、测距来源和误差估计；
- 辅助视觉链不发布 `selected_target`、`drop_ready`、`release_evidence`、旧 `/detect/*`、
  Servo 或任何释放许可；只有 `uav_vision_eval` 内的主动 A/B 导航外挂在评测运行中独占
  `/fastplanner/goal`，用于量化路线是否真正缩短，不属于视觉交付接口；
- 辅助候选不得直接触发下降或投递，目标进入下视视场后必须由现有下视链重新确认；
- 本分支未修改 `patrol_control`、Mission Manager、Fast-Planner、`actuator_pwm`，也未改
  原 `iris_mid360_downward_camera/model.sdf`；
- 是否继续本方向，不只看提前发现两个目标：必须在随机布局、部分目标不可见和红十字场景
  中保持最终发现能力，并证明搜索时间/路径有足够大的净收益；失败时应能退化为下视覆盖。

## 2. 安装角度和视场

俯角定义为光轴相对水平面向下的角度；90° 是现有垂直下视。原型统一使用
640×480、水平 FOV 60°，由 4:3 画幅计算得到垂直 FOV 46.826°。

| 俯角 | 2 m 前向地面范围 | 3.5 m 前向地面范围 | 中心行横向宽度（2/3.5 m） | 判断 |
| --- | --- | --- | --- | --- |
| 45° | 约 0.8～5.0 m | 约 1.4～8.8 m | 3.27/5.72 m | 最远，透视和地面高度误差最敏感 |
| 55° | 约 0.41～3.25 m | 约 0.72～5.69 m | 2.82/4.93 m | 首选平衡点 |
| 60° | 约 0.2～2.7 m | 约 0.4～4.7 m | 2.67/4.67 m | 定位较稳，提前收益较小 |

表格可由下列命令复算，不依赖手工抄值：

```bash
python3 vision_ws/src/uav_vision_eval/scripts/oblique_geometry.py \
  --angles 45 55 60 --heights 2.0 3.0 3.5
```

推荐安装：机体前部中心线、55°向下、位置约为机体系 `(0.12, 0, 0) m`。实际位置不是
硬件定值，必须由安装件和实测外参覆盖。双目基线保持水平；相机不得被桨叶保护架、货物
或投递机构遮挡。3.5 m 是仿真最高常用搜索高度，不把 4 m 规则上限当默认巡航高度。
不采用携货状态下让飞机大角度倾斜来转动下视相机的方案。

## 3. 原型架构

```text
下视相机（现有生产链，不改）
  六分类 -> 圆环/红十字/H -> 精地图点 -> stable ID -> 对准/释放证据

斜下相机（本分支评测链）
  YOLO 粗检测 或 OpenCV 蓝环匿名粗发现
            |
            +-- mono：CameraInfo + TF + ground_z 射线求交
            |
            +-- depth：框中央 ROI 深度中值/MAD + 反投影
                        深度无效时可回退 mono
            v
  /uav_vision/aux/detections_mapped
            v
  独立 aux_candidate_memory（只发布粗候选数组）
            v
  uav_vision_eval 临时导航外挂（仅主动 A/B）
            v
  DOWNWARD_VERIFY -> 现有下视候选/排队/投递链
```

不能把两路异步检测直接喂给现有同一个 `target_memory`。生产 `target_memory` 会把每个输入
数组视为一帧完整结果，辅助相机空帧会错误清零下视连续命中。本原型因此使用评测包内
独立记忆，且不产生最高优先级选择。

## 4. 算法逻辑

### 4.1 粗检测

- 笔记本仿真复用 2026-07-14 `merged_standard` 六分类 `best.pt`；
- 只要求类别框和置信度，不要求蓝环、红十字黑外圈或投递级连续证据；
- 当前生产下视模型不被覆盖、重训或替换；
- 若完整斜视矩阵召回不足，后续只能新增独立斜视数据/模型候选并做 A/B，不得直接回写
  下视生产模型。

双相机并不要求同时运行两个 YOLO 实例。推荐的真实部署结构是一个常驻 RKNN/YOLO
上下文按任务阶段切换输入源：

```text
AUX_SEARCH          只处理斜下相机
APPROACH            斜下为主，预计进入下视范围时短暂交替或切源
DOWNWARD_VERIFY     只处理下视相机
DROP_ALIGN          只处理下视相机和几何精修，关闭辅助链
VERIFY_TIMEOUT      回辅助重捕获，或从保存的覆盖索引继续搜索
```

相机可同时发布图像，但检测器只消费当前 `active_camera` 的有界队列，切源时清空旧帧；模型
权重常驻内存/NPU，不随相机切换重新加载。只有同一权重无法兼顾斜视和下视域差时，才比较
两套权重；即便两套权重也可按阶段串行加载/调度，不默认双模型常开。

### 4.1.1 OpenCV 蓝环低资源基线

- HSV 蓝色分割、形态学和椭圆质量评分只回答“这里像标准投放区”，不能识别中心图案类别；
- 适合 5～10 Hz、低分辨率运行，计算量远低于第二个 YOLO；
- 蓝色干扰、曝光和白平衡会影响真实场地鲁棒性，因此不能因干净 Gazebo 结果直接冻结；
- 若实拍 precision、有效距离或误导航代价不达标，应切回辅助 YOLO，而不是坚持 OpenCV；
- 红十字只有黑色外环，不属于蓝环检测范围。

### 4.2 单目地面求交

1. 用 `CameraInfo` 将 bbox 中心变成相机光学射线；
2. 用源图像时间戳查询相机到 `camera_init`（固定 L1 使用 `world`）的 TF；
3. 射线与 `z=ground_z` 平面求交；
4. 射线接近水平、交点在相机后方、超出最大范围/场地或 TF 过期时拒绝；
5. 用地面距离、射线入射角和地面高度误差给出粗略 `position_uncertainty_m`。

这条路线不需要深度相机，但依赖可靠外参、姿态和地面高度。场地有坡度、靶标不在同一
平面或俯角过小时，误差会迅速增大。

### 4.3 深度 ROI 测距

1. 在检测框中央 35% ROI 取合法深度；
2. 记录有效像素比例、中值和 MAD；
3. 用 bbox 中心射线和中值深度反投影，变换到统一地图系；
4. 深度离散过大、与地面残差过大或时间差超过 0.12 s 时拒绝深度；
5. 参数允许时回退单目，并在 `range_source=mono_fallback` 中显式记录。

Gazebo RGB-D 只代表理想几何上限。它不能证明室外双目在阳光、低纹理、反光地面和快速
运动中仍有同样有效率。

### 4.4 独立粗候选记忆

- 默认连续 2 帧、类别置信度不低于 0.45、地图质量不低于 0.10 才确认；
- 地图距离 1 m 内融合，标准五类允许类别累计投票，红十字单独匹配；
- 只发布 `TargetCandidateArray`，不发布 `TargetCandidate selected_target`；
- TTL 默认 5 s；该 ID 只在辅助链内部有效，不能冒充下视生产 stable ID。

## 5. 话题与消息

| 话题 | 类型 | 含义 |
| --- | --- | --- |
| `/uav_vision/aux/detections` | `TargetDetectionArray` | 隔离的六分类原始框 |
| `/uav_vision/aux/detections_mapped` | `TargetDetectionArray` | 带粗地图点的观测 |
| `/uav_vision/aux/projection_diagnostics` | `uav_vision_eval/AuxProjectionArray` | 方位、俯仰、测距来源、误差和拒绝原因 |
| `/uav_vision/aux/targets` | `TargetCandidateArray` | 独立粗候选记忆 |
| `/uav_vision/aux/perf` | `DiagnosticArray` | 笔记本 detector 性能 |

`AuxProjectionArray` 是评测包本地消息，不是已经冻结的组间接口。未来若导航组确认接入，
公共候选至少需要：相机来源、方位角、粗地图点、测距来源、位置不确定度、源图像时间戳、
TF 年龄和无效原因。本分支没有擅自修改 `uav_vision` 公共消息。

## 6. 搜索与交接时序

```text
COVERAGE_SEARCH
  -> AUX_SUSPECT
  -> AUX_COARSE_LOCATED
  -> NAV_APPROACH          # 后续导航组实现
  -> DOWNWARD_VERIFY
  -> DOWNWARD_CANDIDATE_CONFIRMED
  -> 现有高权重中断 / 候选排队 / 投递逻辑
```

生产 `NAV_APPROACH` 只能飞到粗地图点上方的正常搜索高度，不得按辅助相机结果直接下降或
释放。下视相机应在限定时间内形成同类别、地图距离不超过约 1 m 的确认候选；失败时恢复
原覆盖索引。

本分支已经在 `uav_vision_eval` 中实现临时主动外挂，支持原 12 点覆盖、两行稀疏扫描、
匿名粗候选最近邻抵近、下视五类确认、返航和剩余覆盖 fallback。它只用于 A/B，不修改
正式 Mission Manager 或规划器源码。当前实现也暴露出：粗候选位置可见不等于 planner
可达，访问排序和不可达恢复会直接吃掉视觉提前发现带来的收益。

### 6.1 部分目标不在辅助视场

辅助相机不应以“看全所有目标”为前提。推荐行为是：

1. 保存当前覆盖索引，对已发现粗候选按预计新增路程/权重/不确定度排序；
2. 访问候选并用下视复核，成功的目标进入现有记忆与权重队列；
3. 候选耗尽后，从距离当前位置最近的未覆盖航段继续，而不是从头重飞；
4. 连续不可达或复核失败超过阈值时关闭辅助引导，恢复正常覆盖；
5. 对 0、1、2、部分、全部可见分别统计净时间和净路径，部分可见导致绕路时必须如实判负。

### 6.2 红十字处理

- OpenCV 蓝环链不负责红十字；可研究“红色十字 + 黑色外环”轻量提示，但只能作附加线索；
- 推荐辅助 YOLO包含 `red_cross` 斜视数据，使其能产生高权重粗候选；
- 辅助发现红十字后保存搜索索引并立即抵近，但必须由下视 `red_cross` 几何/语义证据复核；
- 复核成功后按现有规则中断搜索并优先投递，复核失败则恢复索引；
- 辅助未发现红十字时，最低限度覆盖航线仍必须让下视链完成兜底搜索。

## 7. 仿真资产和运行入口

原机架文件保持不变。`generate_oblique_vehicle_sdf.py` 在 `/tmp` 运行期复制基线 SDF 并
注入辅助 RGB 或 RGB-D link，可选 45°、55°、60°；run 结束自动删除派生文件。固定矩阵使用独立
静态相机模型，单目和深度读取同一 640×480、60°传感器，避免视场差异污染比较。

### 7.1 构建与 L0

```bash
source /opt/ros/noetic/setup.bash
cd /home/xhj/liftrace/vision_ws
catkin_make -j1

cd /home/xhj/liftrace
SIM_NO_RECORD=1 bash top_level_scripts/sim_run.sh oblique_aux_projection_l0 \
  roslaunch uav_vision_eval oblique_aux_projection_mock.launch
```

L0 检查：单目求交、深度 ROI、无深度回退、连续帧候选，以及辅助节点对控制/释放话题零
写入。2026-08-15 通过日志：`logs/oblique_aux_projection_l0_20260815_003130/`。

### 7.2 固定姿态矩阵

```bash
# 4 个快速 case：55°、2 m、tent/背景、mono/depth
source /home/xhj/miniconda3/etc/profile.d/conda.sh
conda activate rl_drone
python vision_ws/src/uav_vision_eval/scripts/run_oblique_fixed_matrix.py --profile smoke

# 完整 126 case：2 路 × 3 角度 × 3 高度 × 7 场景
python vision_ws/src/uav_vision_eval/scripts/run_oblique_fixed_matrix.py --profile full
```

每个 case 都通过 `sim_run.sh` 产生独立 run 目录；聚合目录生成 `cases.json`、
`matrix_summary.json` 和中文 `matrix_report.md`。完整矩阵尚未运行，不能宣称固定 Gate 通过。

### 7.3 覆盖航线 shadow

下列命令会启动 **SITL 自动起飞和覆盖航线**，但辅助链仍零控制输出，候选执行关闭：

```bash
cd /home/xhj/liftrace
bash vision_ws/src/uav_vision_eval/scripts/run_oblique_coverage_shadow.sh 55 mono false
```

第三个参数改为 `true` 可看 GUI。脚本会生成派生机架并通过 `sim_run.sh` 归档。该入口只可
用于仿真，禁止复用于实机。已完成一组双 YOLO 观测诊断，但两条件均触发 520 s 墙钟
超时；辅助检测 4/5 且在 H 附近出现 panzer/tank 误分类，粗地图误差 P90 达 5.637 m，不能
作为有效 coverage Gate。这组诊断直接促成了 OpenCV 低资源主动裁路和单 YOLO 切源方向。

### 7.4 主动搜索 A/B

```bash
cd /home/xhj/liftrace
bash vision_ws/src/uav_vision_eval/scripts/run_oblique_guided_ab.sh --repeats 3
```

基线实际走原 12 点覆盖；辅助条件使用普通 RGB 斜下相机和 OpenCV 蓝环粗候选，通过评测
外挂实际裁剪路线并由下视生产链确认五类。运行始终无 GUI、无投递、无降落，且每个子运行
经 `sim_run.sh` 归档。它是资源下限/导航收益实验，不排斥后续用单 YOLO 切源替换 OpenCV。

## 8. Gate 和路线取舍

固定矩阵：

- 55°标准靶粗发现 recall `>= 0.70`、precision `>= 0.90`；
- 粗地图误差 P90 `<= 0.80 m`；
- 同时记录深度有效率、`depth_roi/mono_fallback` 比例和误差估计。

覆盖 shadow：

- 至少两个不同目标比下视更早产生合法粗候选；
- 中位提前距离 `>=1.5 m` 或中位提前时间 `>=1 s`；
- 模拟交接成功率 `>=80%`；
- 辅助节点对导航、控制、释放和旧兼容话题零写入。

主动 A/B：

- 相同 world/模型/下视链下，基线和辅助策略最终发现成功率不得下降；
- 同 seed 配对比较搜索完成仿真时间与实际路径，墙钟时间只记录宿主负载；
- 记录粗候选数、下视确认数、不可达、复核失败和 fallback，不能只挑成功样本；
- 辅助未看全目标时仍须完成搜索；若中位净收益不显著或失败率上升，则不得接入生产导航；
- Servo、释放许可、投递和真实执行输出必须为零。

取舍规则：

1. 单目达到 Gate，且交接成功率与深度差距不超过 10 个百分点：采用单目；
2. 深度使 P90 误差改善至少 0.25 m，或交接成功率提高至少 15 个百分点，且真实硬件可用：
   继续双目；
3. 两者都不能稳定提前发现两个目标：停止该方向。

## 9. 当前证据与未完成项

已完成：

- 视觉工作区全量 `catkin_make -j1` 通过；
- L0 断言 PASS，单目/深度/回退/候选/零控制输出均有机器报告；
- 55°、2 m、tent 单目标 L1 烟测：单目和深度均为 precision=1.0、recall=1.0，地图误差
  约 0.018 m；
- 深度补充轮 214 个有效观测中 `depth_roi=207`、`mono_fallback=7`，有效率 1.0；
- 4-case smoke 聚合通过：mono/depth 在 55°、2 m、tent + 背景上均为
  precision=1.0、recall=1.0、地图误差 P90 约 0.018 m；聚合目录为
  `logs/oblique_fixed_matrix_20260815_004850/`。该结果只有一个标准类别，明确不算正式 Gate；
- 派生 SDF 可生成并通过 XML 解析，新增 launch 均可解析；
- 带 MID360 的运行期派生机架在未解锁 PX4 SITL 中同时输出下视 RGB、斜下 RGB、
  CameraInfo 和深度，机器断言 PASS：
  `logs/oblique_derived_vehicle_camera_smoke_v3_20260815_005848/`；
- 人工查看快照确认 55°画面包含完整靶标及前方环境。
- 双 YOLO coverage 观测诊断未通过：
  `logs/oblique_ab_baseline_r01_20260815_012443/`、
  `logs/oblique_ab_aux_mono_r01_20260815_013332/`；两者均到 520 s 墙钟上限，辅助斜视
  存在 H 附近误分类和 P90 5.637 m 粗点误差，因此不推荐两个同模型实例常开。
- OpenCV 固定姿态蓝环烟测 PASS：138 次观测、precision=1.0、粗地图误差 P90 约
  0.013 m，日志 `logs/oblique_blue_static_55_20260815_020302/`；该结果仅代表干净仿真。
- 主动 A/B 第 1 组：基线和辅助均下视确认五类并安全返回；辅助搜索时间从 186.3 s
  降到 97.8 s（缩短 47.5%），路径从 36.78 m 降到 22.01 m（缩短 40.2%）。日志分别为
  `logs/oblique_guided_baseline_smoke_20260815_021319/` 和
  `logs/oblique_guided_active_smoke_20260815_020427/`。
- 第 2 次辅助运行仍确认五类且未 fallback，但搜索 202.1 s，三个候选抵近/返航出现
  planner 不可达，最终 `return_home_unreachable_25s` FAIL；日志
  `logs/oblique_guided_ab_guided_r02_20260815_023017/`。外层 1 h 上限随后中断第 2 次
  基线；后台继续完成的第 3 次基线 PASS（搜索 168.3 s、路径 32.22 m），第 3 次辅助在
  收尾时终止。仍不能形成第二个有效配对。当前只能判“单轮有大收益、稳定性未通过”。

未完成：

- 五类、红十字、背景 × 3 高度 × 3 角度 × 2 路线完整矩阵；
- 当前覆盖航线上的提前发现和交接正式 Gate；不再把长期双 detector 常开作为推荐路线；
- 真实斜视数据、实际辅助相机型号/重量/USB 带宽和室外深度能力；
- OrangePi 上按阶段交替输入的单 RKNN 调度器；
- 导航组确认后的公共候选接口和 `NAV_APPROACH` 实现。
- 五类标准靶和红十字全部随机布设的成对多 seed 世界，以及辅助视场 0/部分/全部可见分层；
- 红十字斜视数据、辅助 YOLO 与下视复核/高权重中断的端到端验证。

因此当前结论是“辅助视野存在显著节时潜力，但当前固定坐标访问策略不稳定”，不是“已经
证明需要增加第二相机”。下一 Gate 应新开单一里程碑分支实现单 YOLO 双相机切源和全随机
全靶标成对 A/B；OpenCV 保留为低资源对照，不作为唯一方案。
