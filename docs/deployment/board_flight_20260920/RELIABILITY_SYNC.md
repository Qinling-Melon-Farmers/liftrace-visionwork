# FSM与起飞首轨迹修复的板端配置同步

导航权威来源：`liftrace-controlwork`分支`feat/high-view-liveness-20260919`，提交`151eeba`。本次同步到现有板端试飞分支，没有新开分支，也没有连接或部署香橙派。

四套板端专项共同继承：

- 所有从未获得有效轨迹的规划动作使用12秒首轨迹期限；原search-only 12秒兼容保留。
- traj_server进展发布、目标身份检查和FSM SERVER保持恢复启用；0.25秒去抖，和4秒/4cm物理无进展共享每目标两次预算。
- A*目标方向起步原语和拒绝分类诊断进入板端规划源码，安全占据检查不变。
- 高位专项在固定起飞系`(0.60, 0.05)`、1.4m AGL先低空进场，再同点升到2.6m；其余三套不增加高位航段。

离线验证：68项板端/导航定向测试通过；四套preview/flight共8个应用展开通过，均核对通用首轨迹期限、SERVER保持、进展发布、目标身份和高位staging。C++源码与整机研究分支相同，已在整机工作树完整编译并通过plan_manage/path_searching测试。

该同步尚未重新上板，不改写`deployment/onboard_obstacle_reference_20260920`历史快照。下次部署前仍需重新编译板端工作区，并先以preview核对参数；不得把本地测试写成实机修复已验收。
