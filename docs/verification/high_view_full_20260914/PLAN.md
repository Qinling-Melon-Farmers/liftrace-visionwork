# 高位三目标完整链条对照
日期：2026-09-14。仅 feat/high-view-search-research，禁止直接替换正赛部署。

## 已授权实现
高位2.6m闭合内环完整遍历；累计获得唯一、稳定的 red_cross/panzer/bridge 地图线索后提前中断。沿已验证上升柱回降至1.4m，使用当前感知占据栅格枚举最多三点访问顺序，成本包括起点、目标间及最后目标至走廊入口。不执行牛耕覆盖。实际轨迹仍经Fast-Planner自主避障。高位线索仅导航，低位必须重新获得新鲜视觉证据后复用原三槽投递/恢复事务，随后原走廊过门/H降落。
高位路线结束仍缺目标、地图过期或无可达排序均明确失败，不伪装成完成。栅格顺序最优不等于连续空间实际飞行最短；未知自由空间由原规划器验证。

## 对照
先新鲜seed32原navigation_horizontal_search_vcl06，再相同world/field_config/model运行full_strategy。保留原整场Gate，统计任务启动→第三次ACK、任务启动→降落完成，得分、碰撞、目标集合、距离、阶段开销、实际高度。单图单轮仅作方向性结果。任一未完整完成时不能声称整场净节时。
运行必须sim_run单实例并收尾，关闭录屏/bag；保留可复盘的小型产物。原始run索引、结果与图表待实跑填写。

## 实现验证
任务核心/原runtime/单线索/完整策略92项测试通过，两个Catkin工作区构建通过；完整入口节点展开保留navigation_vcl06_assertion及唯一manager/bridge。新增十项覆盖提前中断、缺目标拒绝、低位新鲜证据、三次释放后走廊恢复及感知栅格绕障/排序。

用户补充：两组配对，共四轮，seed32与seed34分别baseline/strategy；每轮完整航迹、高度、速度、目标事务和阶段时间图，另含同seed航迹叠加及配对差值。

## 2026-09-14 用户反馈后的冻结策略
研究主问题：先高位找齐三目标、再低位投递，是否获得整场净收益。
高位仍保留三次独立源时间观测和类别投票；最小观测间隔0.25→0.10s、跨度0.5→0.2s，导航线索不确定度上限0.25→0.45m，高位候选连续计数要求3→1（线索表自身仍须三次观测）。低位新鲜度、连续确认及释放契约不变。
中断位置立即计算包含末端走廊成本的三点顺序，当前位置感知柱无膨胀障碍时就地下发下降目标；受阻则在1.5m内最多25个候选中选附近下降点，三维规划器验证实际运动。不再强制回起飞位置。无可行候选显式失败。单剩余目标无需排序，直接由原三维规划器验证可达性，不因二维排序网格无解而取消其执行。
开发轮7b4eb82在155s附近二投后因排序网格无解中止，0碰撞，完整图见development/；保留该失败，不列为成功。此前随机门接线中止记录见excluded_runs.json。接下来冻结修订版，进行两seed四轮完整配对，所有新失败也必须列入结果。

## 本批完成记录：2026-09-14
四轮冻结对照已结束，不再续跑本批。高位seed32/34均完整37/37 PASS；基线seed32 PASS、seed34近地降落保护圈碰东墙FAIL。两组三投分别节时55.8%/43.2%；只有seed32存在成功整场配对，节时33.8%。失败不以停止时刻伪装为完成时间。
高位优先方向值得继续验证；后续独立提速、更多布局和近地故障复核，不替换正赛部署。完整航迹、指令高度、实际高度、速度、阶段和失败局部图已输出；数据源与复现见docs/verification/high_view_full_20260914/REPORT.md（仓库相对路径）。
