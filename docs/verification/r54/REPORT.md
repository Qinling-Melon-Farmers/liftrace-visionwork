# R54 航点引导单次验证收尾

## 改动

正式运行代码为精简整机分支 `8dbc6da`，导航来源 `085548a`。参考旧成功路线中心线及门前/门后坐标，保留 1.4 m 搜索高度；在原南侧过渡点降至 1.05 m，再向前 0.5 m 降至 0.75 m 建立低位观察点；门后清空点向北延伸 0.2 m 后再爬升。路线由 9 段改为 11 段，门洞评分航段同步为 5/8/10。未改规划器、地图膨胀、运动期限、到达标准或通信链。

## 单次实跑结果

- 唯一正式 run：`logs/r2026_integrated_r54_guided_entry_20260907_023929/`；完整入口、seed 11，与 R53 使用相同模型、定位、安装及搜索配置。
- **Gate FAIL：startup_wall_timeout**。原有启动期限为 180 秒墙钟时间，终止前仿真时钟只推进到 0.000 s。
- 飞机未解锁/起飞（bag 中 armed_seen=False），任务决策 0、投递 0、门洞穿越 0。新航点尚未执行，**不能据此判断本次航点方案有效或无效**。
- 包装器自动退出并清理，ROS/Gazebo/PX4/MAVROS/RViz 零残留；未再进行第二轮测试。main 未合并，功能分支保留。

## 验证与运行说明

- YAML 和三门过平面索引检查通过；正式 launch 静态展开 35 节点、11 航点、评分索引 5/8/10。仅配置修改，无编译项变化。
- 参考 R53 现有点云检查了门外空间；门洞在该旧膨胀图中仍占据，低位靠近后的地图更新效果需要实际飞行验证。本轮未进入该阶段。
- 预检查命令曾因 Windows→WSL 引号解析错误误起空 gzserver，随后调用统一停止脚本清理；未启动 PX4 或飞行任务。清理确认后才启动上述唯一正式 run，未把预检查当作飞行通过记录。
- 静态展开首次缺少 PX4 包路径，补齐与正式包装器一致的搜索路径后通过，未启动 ROS 节点。

## 记录

- 本目录保留 Gate、manifest、单轮摘要和清理输出。
- [原始仿真归档](https://github.com/Qinling-Melon-Farmers/liftrace-visionwork/releases/tag/sim/r2026-r54-guided-entry)含 bag、run.log、ROS/PX4 日志及配置展开记录；为未验收预发布，不代表航点验证通过。
- 按用户“仅一次测试，无论是否成功都收口”的要求结束本次工作，保留方案为待验证状态。
