# 历史失败六seed重跑

用户2026-09-21明确选择仅重跑31、32、34、37、38、40，每个一次；旧十轮和本日seed32先导均保留，不计入本矩阵新结果。

- 使用历史冻结场地，2.6m高位策略、既有快走廊和共享边界修正。
- 统一源码，不中途调参、不以补跑替换失败；不同seed按预定顺序串行运行。
- 开启已有跟随与俯视录制；保留完整数据后统一绘图、合成视频、对比分析。
- ROS任务600秒不变；宿主启动600秒、任务墙钟6000秒、外层7200秒。此调整针对仿真吞吐，不是放宽比赛时限。
- 任务FAIL保留并读取原因后运行下一个布局；启动/收尾故障停止批次，不无分析重试。
- 每轮统一sim_run.sh独占/空间预检/收尾；宿主F盘启动前约64GiB可用。
- 状态文件：logs/failed_six_20260921_batch/matrix.json。统一报告待全部结束后出具。
