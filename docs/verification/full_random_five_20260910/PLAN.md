# 全随机五seed矩阵

完成状态：五组均已运行，2/5整场PASS，结果见[报告](REPORT.md)。下文为开跑前冻结的试验定义。

用户于2026-09-10明确要求：基于最新靶子、树、门全随机地图，连续五个不同seed，结束后全面分析并绘制场地/航线航迹/高度等图。五组串行，不另跑先导、不追加补跑、不按结果替换失败，不进行实机动作。

- 预先固定field_seed/scene_seed=31、32、33、34、35；door_seed=1000+seed；obstacle_seed=2000+seed。独立随机流，未根据飞行结果挑选场地。抽样得到LL/RR/LR/LL/RR，本批没有RL，不宣称四组合全覆盖。
- 使用当前simulation_tools导出器、修复后的靶板避墙布设、四组树箱随机位置/朝向、两扇严格0.80m左右门。靶子位置/朝向由实际spawner生成并回读；仍为当前r2026四标准类加红十字的场景定义。
- 五组用逐字相同的experimental_runtime，走廊采用此前实现的固定中线墙前/墙后引导点；门真值只供Gate，具体绕行依赖实际点云与原规划器。不是按门位置生成航点，也不是已验证的固定LR默认路线。
- 保持R64控制、相机、机架、定位融合、任务/释放和降落算法；不在五组之间修复或调参。保守包络55×55×40cm，FC到相机16cm，ground_z=-0.22，搜索1.18local/1.40AGL。提前返航仍关闭，600秒及故障处理保留。
- observe_full_trial=true，与R64矩阵一致；碰撞/落地/完整PASS/600秒或既有wall timeout终止，失败原样统计。旧PASS目录仅复用来源快照格式，不计作本批先导或新运行。
- 统一run_competition_sim.sh→sim_run.sh，单实例互斥与强制收尾；关闭GUI/RViz/机载录像/俯视录像/录屏/全场bag，保留truth/MAVROS/指令CSV、事件、参数、Gate、接触与PX4日志。
- 对比R60/R64注明地图/路线/机架变化，不能解释为严格配对算法增益。逐组展示实际场地、名义航线/实际航迹/指令轨迹、高度与阶段时刻，另附横向统计和失败局部图。

预检与原始批次索引：logs/_artifacts/full_random_five_20260910。结果尚未完成时本文件不表示PASS。
