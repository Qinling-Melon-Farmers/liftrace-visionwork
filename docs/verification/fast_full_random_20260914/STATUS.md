# 提速实验接续状态（2026-09-14）

> 最新范围修订：用户授权完成提速先导，随后仅seed32/34全随机布局×两策略共四轮。取消原十轮矩阵，31/33/35只保留预生成资产，不运行。以下早期十轮计划作为历史保留。

最新实跑：原覆盖提速先导37/37 PASS、348.954s、0碰撞；高位先导FAIL保留，见[pilot/REPORT.md](pilot/REPORT.md)。接下来冻结两种策略共用速度参数，运行全随机32/34四轮。以下较早接续记录按历史阅读。

历史：按用户纠正，验证父进程及roslaunch参数展开子进程均改为既有`rl_drone`的Python3.9.25，12组展开全部通过，1.2m接收上限已确认。接收距离修正仍待实跑；五seed十轮矩阵尚未启动，没有新的成功完赛时间或提速收益结论。

## 解释器入口修正

此前直接调用`roslaunch`，其`#!/usr/bin/python3`使子进程回到系统3.8；激活conda不能覆盖绝对路径shebang。现在`static_check.py`通过`sys.executable`运行roslaunch模块；`check_rl_drone.sh`显式激活既有环境。仅在该环境补装缺失的`defusedxml==0.7.1`，没有修改系统Python。

明文复现（仅展开参数，不启动节点）：

```bash
bash docs/verification/fast_full_random_20260914/check_rl_drone.sh
```

新的`static_validation.json`记录实际解释器、版本和接收上限，覆盖旧补丁前的验证结果。系统3.8的历史异常不能据此推断整套笔记本环境不可用。完整仿真的包装器仍有环境重置，ROS节点各有shebang/编译依赖；后续实跑需明确入口，不能把本次参数展开通过等同于全链运行通过。

## 已落实的改动

- `292e184`：高/低搜索巡航前视1.0m、规划速度上限1.2m/s；目标精细事务仍0.4m；第三投结束后保持快速航行，到达走廊前第一个航点才切0.15m并进入下降/过门。
- 对照2025原始控制配置的1.0m距离参数；保留当前有界轨迹跟踪，不恢复旧终点回退算法。距离参数不是实测速度。
- 研究launch补充控制指令接收上限1.2m，修复默认0.6m拒收1.0m前视指令的问题。该补丁已通过新的全量参数展开，飞行验证待执行。
- 已生成seed31–35全随机树、门、靶场景，每图高位与原覆盖路线各一轮，双方使用相同提速参数。
- 仅研究分支，未替换正赛或板端部署；后续终端命令均用明文。

## 先导记录

| 运行目录（logs下） | 结果 |
|---|---|
| fast_speed_pilot32_20260914_041135 | ROS包解析异常，未起飞 |
| fast_speed_pilot32_cache_20260914_041407 | 高位爬升时command too far：约0.996m对0.600m限制；运动超时，0碰撞，Gate FAIL |

上述两轮均非正式矩阵，不丢弃、不作为节时对照。提速代码114项相关测试及两个工作区编译曾通过；新的12组参数展开已在rl_drone完成，不代替完整飞行验收。

## 历史系统Python诊断（不代表rl_drone验证结果）

系统Python3.8在ROS参数展开中发生不同位置的内部类型错误，且一次SIGSEGV。干净`env -i`、独立字节码缓存仍复现。独立脚本仅反复读写同一份YAML，第4859次出现`StringIO object is not callable`；固定CPU0的参数展开仍出现`dictobject.c: bad argument to internal function`。这已超出任务节点自身执行范围，但尚未确定根因。

`dpkg -V`检查Python3.8及PyYAML未报告包文件变化；检查时约14GiB可用内存、F盘67GiB可用。无证据证明硬件故障，不据此修改系统Python或安装依赖，也不以反复重跑掩盖异常。

诊断脚本保留于忽略目录：`logs/check_python_runtime.py`、`logs/check_clean_ros.sh`。确认无其他工作进程后，已依次重启发行版及完整WSL虚拟机：发行版重启后独立测试第3539次仍失败；完整重启后10000次YAML读写通过，但随后12组ROS参数展开仍报`SimpleKey object has no attribute index`。因此重启未消除间歇性异常，不能据单次PASS宣称环境恢复。仿真已收尾。

## 恢复后的顺序

1. 已在rl_drone通过12组launch参数展开并确认1.2m接收限幅。实跑前核实包装器与节点解释器边界。
2. 通过统一`sim_run.sh`重跑固定树seed32高位完整先导，检查37项Gate、碰撞、实际巡航速度和第三投到走廊入口的速度阶段。前视/最大速度参数不能代替真值航迹测量。
3. 先导通过后冻结代码及两组共用配置，按31–35进行十次全链实验，交替两种策略的先后顺序。失败先分析，所有失败保留。
4. 输出十轮完整XY/三维航迹、指令与实际高度、速度、阶段、三投及完整完赛时间，单列末投至走廊入口耗时；只有双方成功完成的配对可计算整场节时。

此任务仍待完成，不以旧四轮结果替代新的五seed矩阵。
