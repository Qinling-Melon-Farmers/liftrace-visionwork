# 巡航与走廊前提速、五seed全随机对照

> 最终范围：按用户确认，仅做固定布局seed32/34四方案对照；全随机暂不执行。本文件后续内容为原计划历史。结果见[总报告](../high_view_full_20260914/speed_comparison/REPORT.md)。

> 最新范围修订：用户授权完成提速先导，随后仅seed32/34全随机布局×两策略共四轮。取消原十轮矩阵，31/33/35只保留预生成资产，不运行。以下早期十轮计划作为历史保留。

目标是缩短完赛时间。当前仅研究分支，未替换正赛部署。

对照2025原件：Desktop参考控制配置的px4_max_distance为1.0m，源码默认1.2m；轨迹服务器仍曾配置0.4m，但其全轨迹扫描/终点fallback与当前受限前视实现不同。提速采用当前跟踪链的参数与阶段切换。

本轮首个待验证配置：巡航前视1.0m，规划速度上限1.2m/s；目标事务精细阶段前视0.4m；到达走廊前高位导航点后才切为0.15m并开始原下降/过门流程。第三投至该点保持巡航参数。实际速度以真值轨迹测量，不将前视距离或规划上限当作实测速率。

先运行已验证固定树seed32完整高位试飞作为提速先导；根据实际速度、跟踪、净空和阶段读回结果冻结参数。随后使用31–35五套全随机树/门/靶地图，每图各跑高位策略与原低位覆盖，共10轮，交替先后顺序。两组使用同一提速参数、相机、模型和原完整Gate。前视精细切换/三投/过门/降落/实际碰撞全部保留。

场景由既有scene_layout/export工具按seed生成，不筛掉不利布局；树的物理位置只供场景和评测，不提供给导航选靶。随机门使用中性墙前后点和独立Gate几何。三投后两组都停止搜索。

报告保留全部失败，未完成轮不计算成功整场节时；输出每轮完整航迹、指令/实际高度、速度和阶段图、逐投及整场时间、到走廊前的速度与开销、同图差值。记录关闭bag和视频，保留必要原始数据并检查宿主存储。终端命令明文显示，不使用base64。
