# 局部入口真实SITL对照计划

状态：四轮均已完成并收尾，结果为1 PASS、3 FAIL，全部保留；见[完整报告与图表](REPORT.md)。以下为启动前冻结的对照计划。

2026-09-12，用户明确同意进行真实仿真对比验证。本批研究代码先冻结提交，逐轮通过sim_run.sh独占运行并收尾，不替换正赛部署。

| 顺序 | 原全随机布局 | 航带 | 初始规划等待上限 | 建议节点 | 局部入口执行 |
|---|---|---|---|---|---|
| 1 | seed32 | 0.70m | 0，保留原行为 | 开 | 关 |
| 2 | seed32，同图 | 0.70m | 0 | 开 | 开 |
| 3 | seed34 | 0.70m | 20s | 开 | 关 |
| 4 | seed34，同图 | 0.70m | 20s | 开 | 开 |

每对使用相同源码、world/field/runtime/gate配置、模型、机架、高度、速度、观测参考与资源参数，仅局部执行开关不同。seed34使用原0.70m runtime，不继承上一轮0.80m候选。世界和配置来自docs/verification/full_random_five_20260910/seed_{32,34}/scenario_inputs；coverage_config沿用coverage_stage2_20260911/reference_shadow.yaml。

基线先检查建议服务确有有效输出、地图时效和错误原因，再运行该布局执行候选。若接口不工作或发现新错误，先保留该轮并分析、相关修复后重新建立配对，不将无效候选当成算法收益。所有FAIL保留，不覆盖已有历史结果。

比较三投时间/类别、SEARCH+RESUME路程、无轨迹等待、局部接纳/完成/失败/偏移、原deadline与游标、原37项Gate，以及实际航线/高度/碰撞与近地限制。单布局一次对照不作鲁棒性概率。

记录使用SIM_NO_RECORD=1，关闭全场视频和bag；观测采样最多120份、建议JSONL最多1500行。每轮前后检查F盘宿主余量，SIM_STORAGE_GUARD_PATH=/mnt/f，原20GiB下限保持；报告后清理可重建中间产物，保留本批必要原始对照。运行进度先留忽略的logs目录，结果完成后再同步正式报告。
