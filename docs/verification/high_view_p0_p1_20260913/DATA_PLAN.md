# P0下一轮采样与标注准备

用户已批准研究开发，并确认目前没有额外2.4–3米同步素材。本轮没有启动新仿真或实机，以下是下一批数据工作的具体输入要求，不是启动命令。

## 先做小批可行性，再扩大统计

先在已知标定、模型、可观测地面目标和明确净空的条件下比较1.4 m参考与2.4/2.6/2.8 m。目标点不能从world真值送入飞行策略；相机位置可按已知场地边界布置，真值仅在离线评估端使用。

第一批建议固定树32/34两套布局、每高度在数个合法观察位置停稳取样，先判断图案/红十字是否够用；随后对候选高度增加0.8 m/s通过观察段。静止结果不能代替运动检出率。这个小批只决定是否值得扩展，不满足30独立布局的统计门槛。

需要真实相机和RKNN上的对应采样/回放：渲染场景能证明算法契约和仿真观测，不证明反光、真实曝光、运动模糊和板端实时。若先采集SITL，后续报告必须明确域差异。

## 每份原始数据

- 原始RGB及源时间戳，不以带框视频替代原图。
- 同期CameraInfo(K/D/分辨率/相机frame)和完整相机TF。
- 估计与真值（如有）分列，FC AGL计算依据、ground_z、机架外参、滚俯/yaw、速度、曝光信息（若驱动可提供）。
- 当前模型/预处理版本与输出，不能从旧PT结果冒充当前RKNN。
- 同期地图/遮挡摘要、独立layout_id、segment_id。每个观察段至少覆盖一次完整有效视场通过；相邻视频帧不作为独立样本重复计数。
- 每个物理靶的类别、中心真值、可见性/遮挡和最终确认；误检也必须标注，不能只标检测成功的目标。

采集默认不录全场bag/视频，每轮原始抽样预算100MiB；帧间必须保留对应时间，压缩与清理不能损坏同步关系。板端空间、LIO/规划/RKNN延迟同步记录。

## 评估JSON格式

如下只是格式示例，不是实测结果：

```json
[
  {
    "layout_id": "fixture-32",
    "segment_id": "h26-view01-pass01",
    "fc_agl": 2.6,
    "height_verified": true,
    "calibration_id": "实际标定版本",
    "model_revision": "实际模型版本",
    "truth": [
      {"instance_id": "physical-red", "class_name": "red_cross", "xy": [1.0, 2.0], "visible": true}
    ],
    "confirmed_predictions": [
      {"matched_instance_id": "physical-red", "class_name": "red_cross", "xy": [1.1, 2.0]}
    ]
  }
]
```

truth包含观察段预先定义评估范围内的全部目标，含遮挡/漏检；范围固定后不能按检测结果裁剪。matched_instance_id由离线标注或独立匹配产生，线上策略绝不能读取。每个物理实例每独立段只提交一个最终确认，禁止靠重复正确帧稀释误分类分母；未知/错误物理关联用null，计入未匹配确认。

评估目标组合时增加expected_top3与selected_top3，元素是物理instance_id；各含三个不同身份。对于尚未发现足够目标的观察段，selected_top3为空，不能省略失败段以提高成功率。

P0原定门槛见[已批准设计](../../planning/high_view_search_20260913/PLAN.md#11-批准后的阶段与停止条件)。评估器提供点估计和Wilson区间：即使20/20发现，也不能证明总体发现率下限≥95%；零误分类也需要足够独立样本。通过点估计只标记NEEDS_INDEPENDENT_REVIEW。
