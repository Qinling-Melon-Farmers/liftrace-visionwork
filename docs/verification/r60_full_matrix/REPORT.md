# R60 完整 Gate 与十 seed 验证（准备中）

用户已授权完整全场重跑，完整 PASS 后冻结版本执行十种不同靶标布设；仍不录全场 bag。尚未启动新轮次，不能宣称本版本 PASS。

R59 原始 Gate 为 FAIL：8/8 航点、两处严格 0.80 m 通口、H 对准及 AUTO.LAND 触垫已观察，最终 ON_GROUND/disarm 未确认。终点 5 mm 平面 H 垫分类遗漏已修正。本轮保持物理接触和严格尺寸，完整 Gate 继续要求落地和解除武装。

## 集成改动

- 搜索/投递策略、1.40 m 搜索和 1.60 m 投递取景保持原值。
- 投后先在廊外 (-3.091588, 6.292690) 到达 1.40 m，再原地下降至 0.40 m，下降点到达误差 0.05 m；确认到达后才把规划顶界改为 0.55 m、控制指令顶界改为 0.50 m。
- 返程跟随步长 0.15 m、通常到达误差 0.12 m/停稳 0.80 s，沿用 R59 低空路线和墙间横向对齐点；H 取景 0.50 m，开启既有 H 笔画补充。
- 用现有 ROS 参数在任务航点完成边界切换；原消费者增加缓存参数读取，不增加控制节点或话题。局部地图在重建前读取顶界，按原 resetBuffer 清除旧占据。
- 独立评测对整个走廊矩形区域检查实际高度 ≤0.70 m，含两门之间，搜索区仍按完整比赛限高。

整机 Catkin 构建、239 项任务/执行/Gate 回归、launch 参数展开通过。尚待动态完整 Gate 与十 seed。
