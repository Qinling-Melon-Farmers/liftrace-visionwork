# 高空快速先搜五seed验证与工程继承

目标：清理可证冗余分支/worktree；先补齐camera_init↔map修复；冻结高位快速先搜版本，在31–35五套历史场景运行，和2026-09-10低速遍历数据逐seed对比。使用历史归档world/field_config/runtime/gate_geometry，不重新随机生成或筛选有利布局。新运行单列失败，不替换历史失败。只运行五个高位试验，低速对照使用已有数据。

继承核实：研究HEAD源于综合工程86e382d，原来未包含板端独立71827d9。已移植其双向适配器、动态camera_init→map对齐和硬件launch接线，保留时间戳/体坐标twist/协方差约束；新增对齐初始化与重置回归。硬件节点不在仿真默认启动，不将同坐标仿真当作非单位实机TF验证，不上板或替换正赛代码。

清理：只删除已被保留分支完整包含的旧引用；远端额外限定已合入origin/main（PR API不可读，不动未合入主干的远端开发分支）。保留main、当前研究、综合基线、板端、地图记忆独有分支及原始基线。退休综合工程和地图记忆两个旧worktree，先归档其忽略的非构建资产及报告，保留旧路径兼容链接；不删除脏基线或独有板端工作。

试验：统一sim_run互斥和强制收尾，rl_drone启动器，无GUI/视频/bag。五组结束后汇总完整航迹、高度、速度、投递、门/降落及失败阶段。保留之前保守投影疑点，不能把飞行Gate通过等同所有规则/实机验收。
