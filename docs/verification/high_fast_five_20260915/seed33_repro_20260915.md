# seed33 高位快速先搜复现（2026-09-15，HEAD 59c6215）

用户当日明确授权的单轮复现；仿真接管后首轮。目的：确认当前研究 HEAD 上 seed33 高位实验能否重现 09-15 同场景 PASS，不作稳定性宣称（仅 1 轮）。

## 配置与等价性

- 运行目录：`logs/seed33_repro_20260915_20260915_170835`（17:08:35–17:36:46，Gate 终止后统一收尾，零残留 PASS）。
- 场景输入 `seed_33/{field.world,field_config.yaml,runtime.yaml,gate_geometry.yaml}` 与 09-15 批次逐字节一致（`git diff 20b1a0f..59c6215` 该目录为空）；同一 best.pt；同一入口 `uav_high_view fast_comparison.launch strategy:=true field_seed:=33`；驱动脚本 `reproduce_seed33.sh` 复刻 matrix.py 单轮命令/环境（SIM_REQUIRE_GATE、无录像、2850s 上限）。
- 代码差异：HEAD 20b1a0f → 59c6215，多出 d463613（持久线索/有界换点/低位兜底）与 a12750b（下降解耦/逐点排序）；devel 为 relay 式引用，当前 src 即运行代码，无需重建。

## 结果

**Gate FAIL：`corridor_height_limit_violation`**（t=385.785s 由 Gate 主动终止）。此前的任务链全部成功：高位搜索聚齐三类并中断、red_cross/bridge/panzer 三投与三次恢复、投后 9 点路线 9/9、按序过 corridor_entry/Wall_20/Wall_22 两门，0 碰撞、0 越界。失败检查 8 项均为终止后果（未降落/未 disarm/任务未完成等）。

## 首个失败阶段定位

- Gate 判定（navigation_vcl06_assertion.py observe_pose）：低空区 x∈[-4.8,4.8]、y∈[7.6,9.1] 内 `pose.z - ground_z > 0.7`，ground_z=-0.22，pose_source=gazebo_truth，与原轮同一评估基准。
- 真值复核：违规窗口 t=385.783–385.994s 共 3 个采样，峰值 **AGL 0.7114m** 位于 (3.83, 8.06)；同区域 09-15 原轮峰值 **0.6236m**（0 采样超阈）。仅超阈约 1.1cm。
- 航迹还原：t=374–385s 在 H(4.2,8.5) 上方 0.39–0.51m 对准徘徊；随后向西南 (3.8,8.0) 移动并爬升越过阈值——即末段 H 视觉降落对准/重调阶段爬升触顶，不是走廊过门阶段（Wall_20/Wall_22 过门 z 均 <0.7）。
- 使命在 t=375.6s 才发出 LAND；未完成降落与 disarm。

## 对比与边界

| 项 | 09-15 原轮(20b1a0f) | 本轮(59c6215) |
|---|---|---|
| Gate | PASS 37/37 | FAIL（末段走廊限高） |
| 任务 ROS 时长 | 224.367s 完成 | 385.8s 终止时未完成 |
| 三投 | 64.8/83.0/96.1s | 已完成（时刻不同） |
| 走廊区真值峰值 AGL | 0.6236m | 0.7114m（超 0.7 阈值） |

- 单轮样本，不能排除仿真时序波动；不能据此断言 d463613/a12750b 造成回归（两修复触及下降柱选择/线索保持，不触及 H 降落对准爬升逻辑），也不能断言无关。
- 0.7m 为工程阈值（09-14 已记录同族 FAIL 与"走廊工程阈值另行处理"），规则隔离墙高 1.5m；未按阈值放松处理，原 FAIL 保留。
- 按规则 19 本轮后不自动重跑；同 FAIL 重跑需先完成代码/配置改动或用户明确要求测波动。

## 下一步建议

末段降落对准爬升与走廊低空区 0.7m 工程阈值的矛盾单列：可选方向为 H 重调爬升上限/区域感知减速、或复核阈值与规则 1.5m 墙的对应关系；与路线图中"走廊轨迹保持/重规划衔接"和"靠墙末段减速余量"两项并列跟踪。