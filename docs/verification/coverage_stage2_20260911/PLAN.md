# 已授权：历史布局在线验证与后续研究开发

2026-09-11用户明确批准必要历史布局验证和后续阶段开发。工作仅在`feat/coverage-efficiency-research`；正赛整机工作树86e382d和交付c369e6f8保持隔离。

首轮选择全随机seed32：原图此前完整PASS，同时包含重复北侧/南侧绕行，适合先检验在线旁路输入和覆盖估计。加载原`full_random_five_20260910/seed_32/scenario_inputs`的world、field_config、runtime、gate_geometry，0.70m航带、等待上限0及原控制参数保持。新增研究观察器与限量同步采样，不增加独立纸箱；四组树下垫箱保持历史尺寸。

新观察器在本研究入口required，失败时结束整套受管运行，修正后才允许下一轮。每轮通过SIM_RUN_AUTHORIZED=1 + sim_run.sh，单实例，成功/失败/中断统一收尾；首轮不因结果差而原样刷轮数。后续轮次按实际问题和已完成改动决定，不以旧计划48轮作为自动配额。

在线留存：原关键CSV/事件/参数/Gate；旁路status.jsonl，最多120份、间隔至少6 ROS s的同步PNG、CameraInfo/图像时刻TF、已选占据点云及状态栅格。缺数据的字段留空，不读真值补齐。无桌面录屏、全场视频或bag。

按顺序检查：观察器输入/时序是否正常；实际图像质量与遮挡样例；地图记忆的错误累计/失效；最后再决定航带或调度候选。同步样本用于离线重放和标签核对，真值只用于独立评测。不能仅因重复路线少就宣称召回或得分提升。

后续开发以实跑事实为依据：修复暴露的旁路接口/质量问题，补地图有效性与安全的覆盖查询/补扫建议，再在必要布局上验证；尚无可靠覆盖证据时保持建议旁路，不用不可信记忆跳点。所有原始FAIL保留，最终汇总航线、覆盖图、高度、时间和未完成项。
