# R64拓扑/计划任务后的本地清理

2026-09-09。用户明确要求清理冗余worktree、临时文件和缓存；本次只在WSL已核对路径执行，未触碰Windows其他盘的文件。移除本地文件逻辑大小合计8,397,663,125字节，即约8.40GB / 7.82GiB。此数不代表宿主VHDX已缩小。[汇总JSON](r64_cleanup_20260909.json)。

| 类别 | 移除字节 | 处理 |
|---|---:|---|
| pip下载缓存/旧tmp日志/字节码/已安装工具链下载包 | 6,556,127,091 | 不删除conda环境、已安装编译器、权重与实拍图像 |
| ROS中央日志重复副本 | 266,962,038 | 1461份文件逐文件内容匹配归档后移除；不是清空全部ROS记录 |
| 导航来源旧build/devel | 566,212,981 | 缓存原引用已删除视觉worktree；导航源码和分支保留 |
| 三个冗余worktree剩余文件 | 1,008,361,015 | git worktree remove；干净状态、提交已在远端，分支不删除 |

移除的checkout：r2026-main-integration、r64-main-acceptance、vdeploy-final-closeout-plan。对应分支与main合并/tag仍在远端。

旧视觉worktree独有日志257,798,834字节已完整迁至当前工程`logs/_archive/vdeploy-final-closeout-plan/`，未计入删除量。历史报告中的旧绝对路径可按此目录查找，内部run目录名不变。R64全部11轮相机视频、seed11整场俯视视频、PASS/FAIL原始记录和当前部署/仿真包均保留。

保留当前整机工作区`r2026-competition-integrated`的两套可用build/devel；清理后已重新source并用rospack确认uav_vision与uav_mission解析到当前目录。保留原基线`/home/xhj/liftrace`的用户改动和原始资产，保留导航来源`vcl06-local-full-mission`，保留有未提交修改的`vcl06-planner-stop-ack`；轻量仓继续在`/home/xhj/liftrace-sim`。

独立导航来源worktree若以后需要编译，使用当前视觉overlay，不再引用已移除的vdeploy目录（此命令仅作后续说明，本次未执行）：

```bash
source /opt/ros/noetic/setup.bash
source /home/xhj/liftrace-worktrees/r2026-competition-integrated/vision_ws/devel/setup.bash
cd /home/xhj/liftrace-controlwork-worktrees/vcl06-local-full-mission/patrol_uav_ws-patrol_planner
catkin_make -DROS_EDITION=ROS1 -j1
```

已成功执行`fstrim /`通知WSL文件系统空闲块；其输出的922.7GiB是整个文件系统可discard区间，不能当成本次释放量。trim前后宿主F盘可用字节没有增加，因此没有声称向宿主释放8.40GB。未关闭WSL、离线压缩VHDX或清理其他盘，避免中断当前环境。

三版rqt图和优化计划已在远端main以--no-ff合并到ce63b8d，保留feat/competition-r64-topology-plan，标签chore/r64-topology-time-plan。轻量fork文档更新为69e66c4，既有PR随该分支更新。这里只追加本地维护记录，未改飞行代码或重打包。
