# 视觉组对导航组的当前联调需求

更新时间：2026-08-31

适用分支：`feat/vcl06-next-integration@db80dfd6d22f31ab682d02ebb39a52a1e082384e`

阅读规则：第 13 节是当前合并态，优先于第 2、7、9、11、12 节中保留的阶段请求。

相关回复：[视觉组对导航组 V-SIM-04 扩展需求的阶段回复](./视觉组对导航组V-SIM-04扩展需求阶段回复_20260829.md)

## 1. 目的与来源冻结

本文基于当前联动分支已经跑出的接口和 Gate 事实，整理视觉组现阶段需要导航组提供的任务层
能力。它不是要求导航组承担视觉召回、地图投影或相机标定，也不是要求视觉组在 adapter 中
复制第二套导航策略。

两个导航来源必须分开记录：

- 正式搜索 manager：`liftrace-controlwork@5144aa8f536bdcd214aea2f39ada558383b3bcb0`，当前联动
  分支保持上游源码逻辑不改；
- 地图实验：`liftrace-controlwork@a68925d15293e5510e2b4351c6b3d9bc5aa136ab`，仅含 6 棵 Oak
  tree 和 3 座 radio tower 场外定位特征，不含 manager、接口或调度更新。

2026-08-31 新增第三层来源：本地 clean 导航任务实现由 manager/runtime `a65a616` 与
planner telemetry `022b763` 组成，以 `7dd2c49` 导入本仓；与 start gate、视觉冻结上下文和
typed evaluator 合并后的当前状态为 `db80dfd`。它不改变外部远程仍为 `a68925d` 的事实。

历史已实跑随机场 launch 中，旧导航 manager 发布 `/navigation/goal_raw`，adapter 是唯一
`/fastplanner/goal` 和 `/mission/command` 发布者；临时 `coverage_search_manager` 只保留
V-CL-05 回归。当前 `db80dfd` 已改用 typed `/navigation/mission_command_raw` 作为新 manager
输出，但 execution bridge 尚未合入，因此该新链还不能称为正式可运行 launch。

历史运行数据流为：

```text
/uav_vision/targets
  -> profile_candidate_selector
  -> /mission/profile_selected_target
  -> target_search_manager_py
  -> /navigation/goal_raw
  -> navigation_visual_delivery_adapter
  -> /fastplanner/goal + /mission/command
```

manager 和 adapter 都通过 launch remap 消费 `/mission/profile_selected_target`；视觉原始
`/uav_vision/selected_target` 仍保留用于视觉侧统计和 raw/profile 双层 Gate 审计。

新核心的目标数据流冻结为：

```text
/uav_vision/targets
  -> navigation_mission_manager
  -> /navigation/mission_command_raw (NavigationDecision)
  -> live execution bridge（待合入）
  -> /fastplanner/goal + /navigation/mission_result
```

## 2. 当前事实与主责判断

### 已接通的能力

- 视觉已发布 `/uav_vision/targets` 长期候选记忆和 `/uav_vision/selected_target` 当前新鲜
  排序提案；后者要求连续 3 帧、地图/关联有效、无拒绝原因、年龄不超过 0.5 s 和 profile
  准入。
- adapter 已能把导航 raw goal 与仍然新鲜的候选空间匹配，实际发布
  `MissionCommand.APPROACH`，随后进入捕获、对准、安全 mock ACK、恢复和返航链。
- field/anchor/contact/pose/control/map READY 屏障、唯一 goal 发布者和 30 s 预检已经可验证。
- `r2026` 当前只准入 `tent/pillbox/bridge/panzer/red_cross`；tank 资产、权重和 `full` 回归
  保留，但正式 r2026 raw/profile selected 与 accepted 必须为 0。

### 当前不能由视觉接口替代的缺口

旧 `manager@5144aa8` 的确存在“接受即永久去重、无持久队列/结果回流/重试/剩余时间调度”
问题；该判断只适用于历史运行链。当前本地 clean 导航任务核心已经实现持久队列、typed result
reducer、有限冷却重试、三槽、510 秒硬返航和 600 秒任务上限，并通过纯测试。

因此，**当前跨组主要代码缺口已转移到 live execution bridge 和运行接线**：需要把 typed
decision 唯一转换为 `/fastplanner/goal`，把 planner/target-stage `STARTED` 和分阶段 result
回流新 manager，并验证高度/轨迹执行。视觉组仍主责候选质量、工作点邻域性能和 CameraInfo
接入；这两类缺口不能互相代替。

## 3. 职责边界

| 能力 | 视觉组 | 导航组 | 联合验收 |
| --- | --- | --- | --- |
| 候选观测 | stable ID、类别、地图点、质量、关联、拒绝原因、`last_seen` | 校验任务可达性和当前状态 | 同一 ID 可追踪 |
| 当前排序 | 发布新鲜 `selected_target` 提案 | 决定接受、忽略或延后 | 接受事件不能由 selected 推断 |
| 长期候选 | `/targets` 保留粘性记忆，不伪造新鲜度 | 持久队列、优先级、终态、冷却和重试 | 失败后行为可复现 |
| 搜索与接近 | 提供性能曲面和地图候选 | 航线、航高、航速、planner goal、恢复搜索 | `P_interrupt` 与轨迹关联 |
| 投递结果 | 对准/释放视觉证据 | 任务结果、槽位、重试/终止决策 | 三个不同 ID 按槽位完成 |
| 相机与坐标 | CameraInfo、optical frame；与飞控/结构组联合标定安装外参；TF 失败关闭 | 与定位/飞控共同冻结权威 mission pose/TF，并按合同消费 | 同时间戳、同 frame |
| 安全与时限 | 不发布飞行/机构命令 | 高度、碰撞/边界、10 min 调度、返航/降落 | 600 s 硬 Gate |

## 4. 导航任务语义合同（核心已实现，live 接线待验收）

第 4.1～4.4 节是形成 clean 导航核心的需求依据。`a65a616` 已按 typed
`NavigationDecision/NavigationResult` 实现这些策略并通过纯测试；当前不再要求重复实现，
只要求 execution bridge 无损执行并用运行事件证明。

### 4.1 把“立即中断”和“稍后调度”分开

导航 manager 需要接收两个任务输入语义，同时保留一个只读审计输出：

- `/mission/profile_selected_target`：当前正式 launch 中由 selector 从 `/uav_vision/targets`
  产生，只代表此刻新鲜、完整准入且 profile 允许的**立即中断提案**；
- `/uav_vision/selected_target`：视觉原始排序结果，保留作算法统计与绕过 profile 的审计，不得
  在正式 r2026 链中绕过 selector 直接触发任务；
- `/uav_vision/targets`：包含跨航段保留的地图候选，导航可在执行同一 profile 过滤后建立
  **持久任务队列**；若改用过滤后的数组，应先冻结其单一发布者和 schema。

不得通过周期性重发 selected 刷新候选，也不得把长期 `CONFIRMED` 状态当成当前连续帧有效。
导航侧队列至少以 stable ID 为键保存类别、地图点、质量、首次/最后观测、尝试次数、最近结果、
冷却截止和终态；选取时再叠加可达性、距离、规则 profile、剩余时间和已用槽位。

### 4.2 不能再用空间邻近反推接受的 stable ID

历史 `/navigation/goal_raw` 是不携带 stable ID 的 `PoseStamped`，adapter 只能在 0.6 m 内用
空间邻近关系绑定候选；该方式只保留作旧链回归。clean 核心已经冻结
`/navigation/mission_command_raw` 的 `uav_mission/NavigationDecision`：

- `SEARCH/RESUME/RETURN_HOME/LAND` 携带 goal；
- `APPROACH` 同时携带 `has_target`、`target_id`、`target_first_seen`、类别、attempt、slot 和 goal；
- `mission_id + decision_seq` 提供执行 fence，`target_id=0` 合法；
- live bridge 必须是唯一 `/fastplanner/goal` 发布者，并把 `decision_seq` 写入 goal header；
- `/navigation/goal_raw` 只作旧链兼容，正式多目标 Gate 使用 typed 完整身份绑定。

合同与话题名已经冻结；bridge 不得降级为空间推断，也不得与旧 adapter 同时发布同一个
planner goal 或正式 mission command。

### 4.3 提供候选接受与任务结果回流

视觉的 `selected_target` 是提案，不是导航接受。导航侧至少需要提供以下有序事件：

```text
candidate_accepted
approach_started
approach_failed / capture_failed / alignment_failed / release_failed
delivery_succeeded
retry_scheduled / retry_exhausted
candidate_skipped
mission_returning / mission_completed
```

每个事件至少包含：

```text
schema_version
event_seq
event
stable_id
class_name
previous_state
state
result
reason
retryable
attempt
source_stamp_ros
receipt_monotonic（仅由同机 recorder 记录，不作跨机时间基准）
profile
```

传输形式现已冻结为 `/navigation/mission_result` 的 `NavigationResult`，由
`mission_id + executor_id + event_seq` 幂等去重，并以完整 decision/target/attempt/slot 键关联；
`/mission/target_search_status` 只保留旧 adapter 诊断，不得冒充 typed result。

`P_interrupt` 的当前计数口径为：同一完整键先有合法 decision/dispatch，再由 executor 在显式
`target_stage` capability 下发布严格早于 deadline 的 `STARTED/CAPTURE`。仅出现 selected、
decision、planner accepted/arrival 都不计数；没有 live bridge 时必须为 `null`。

### 4.4 失败、冷却和重试归导航任务层

- 候选不能在“收到即接受”时永久进入 terminal 去重集合；
- `approach_unreachable`、`capture_timeout`、`alignment_timeout`、`release_rejected` 必须区分；
- 可重试失败要有有限次数和冷却，成功/不可重试/次数耗尽才进入终态；
- 同一 ID 重试不得重复占用 payload slot，成功 ACK 后不得重复投递；
- 第三投优先级、停止全覆盖、返航余量和任务超时由导航根据剩余时间调度。

上述持久队列、重试和剩余时间调度不得继续塞进 `navigation_visual_delivery_adapter`。
adapter 当前作为联调执行/安全桥，保留既有起飞屏障、命令转发、抵近后的捕获/对准、安全
mock ACK、恢复、三投、返航/降落、timeout/stall 和联合证据记录职责。

### 4.5 收敛航高、速度与地图 profile

同 seed A/B 已得到：

| profile | 30 s 预检 | 90 s 最大高度 | RMS 高度漂移 | 共同首段耗时 | 结论 |
| --- | --- | ---: | ---: | ---: | --- |
| baseline | PASS | 4.798 m | 1.106 m | 60.01 s | 高度超限，FAIL |
| `a68925d` | PASS | 3.254 m | 0.513 m | 71.67 s | 单轮安全，但比 baseline 慢 19.43% |

比较 Gate 为 FAIL，`promote_candidate=false`。导航组需提供收敛后的航高/速度/路线或规划参数，
同时满足最大高度不超过 4 m、碰撞/越界为 0，并把同进度墙钟退化控制在 10% 内。当前默认保持
baseline 只表示候选未满足推广合同，不表示 baseline 的 90 s 运行安全通过。

若继续使用 `a68925d`，manifest 必须显式声明依赖 6 棵 Oak tree 和 3 座 radio tower 场外高大
特征，并始终保留 `baseline` 回退入口。

## 5. 最小接口合同

### 视觉提供给导航

| 接口 | 语义 | 导航侧要求 |
| --- | --- | --- |
| `/uav_vision/targets` | 粘性地图候选集合 | 以 ID 建队列；重新检查 frame、质量、任务终态，不把重发当新观测 |
| `/uav_vision/selected_target` | 视觉原始当前排序 | 用于视觉统计和 raw/profile 审计；r2026 不得绕过 selector 触发任务 |
| `/mission/profile_selected_target` | 当前 profile 过滤后的新鲜提案 | 正式立即中断输入；接受后必须回流明确事件 |
| `/uav_vision/reset_memory` | 清空视觉任务记忆 | 仅在任务/地图边界调用并记录结果 |
| `/uav_vision/drop_offset`、`drop_ready`、`release_evidence` | 末端视觉观测/证据 | 由任务安全层组合为许可，不能直接驱动执行机构 |

正式候选输入条件维持：`state==CONFIRMED`、当前连续帧不少于 3、`map_valid`、mission frame
匹配、`association_valid`、无拒绝原因、所有数值有限、`0<=age<=0.5 s` 且 profile 允许。

### 导航需要返回

| 输出 | 最小语义 |
| --- | --- |
| 带 ID 的 raw decision | 明确 SEARCH/APPROACH/RESUME 等命令、stable ID、类别和 goal |
| 接受事件 | 说明何时、为何接受哪个视觉提案，不能由视觉反推 |
| 结果事件 | 成功、失败阶段、原因、是否可重试、尝试次数和终态 |
| 队列快照/摘要 | 当前 pending/cooldown/executing/terminal ID 与剩余时间 |
| 导航运行 manifest | manager revision、地图 profile、路线/航高/航速、阈值和模型/seed 身份 |

## 6. 联合 Gate 与进入条件

### 30 s 预检

- 固定 world、非零 seed、`r2026`、模型、CameraInfo/TF 和两个仓 revision；
- 随机真值、footprint、field/anchor/contact、pose、map 和所有必需 publisher 持续有效；
- manager 是唯一 raw decision 发布者，adapter 是唯一 planner goal 发布者；
- profile selector 是唯一 `/mission/profile_selected_target` 发布者；
- 0 碰撞、0 越界、无 readiness dropout；必须生成 `gate_status.json`。

### 90 s A/B

- 同 world、seed、真值、模型、路线和 debug-off；
- 双方都安全，最大高度不超过 4 m，0 碰撞、0 越界、无 pose 长断流；
- 规划失败或高度漂移至少一项改善；同进度墙钟退化不超过 10%；
- 未满足全部条件不得切换默认，也不得进入 600 s Gate。

### 600 s 联合硬 Gate

- 三个不同 stable ID 依次按 payload 槽位 1/2/3 完成 software mock 投递；
- 每个 ID 都能关联 confirm→selected proposal→accepted→actual interrupt→result；
- 三投后返航并落地，0 碰撞、0 越界、0 错误释放，最大高度不超过 4 m；
- r2026 下 tank raw/profile `selected=0`、`accepted=0`；
- manifest、逐事件日志、最终状态、`gate_status.json` 和规定报告齐全。

如果 Gate 暴露队列、重试或时间调度缺口，应保持同 seed FAIL 并回流导航仓，不在 adapter
复制任务策略。当前 A/B 未通过，因此 600 s 暂不启动。

## 7. 希望下一次集成交付的内容

1. 审核并合入 live execution bridge，明确其 source revision；不得把未审核工作树内容混入。
2. bridge 是唯一 `/fastplanner/goal` 发布者，并把 `decision_seq` 原样写入 goal header；
   manager 仍是唯一 typed raw decision 发布者。
3. 回传 typed `ACCEPTED/STARTED/PROGRESS/terminal`，至少覆盖 PLANNER、CAPTURE、ALIGNMENT、
   RELEASE、RECOVERY；真实 `P_interrupt` 只由 target-stage `STARTED/CAPTURE` 计数。
4. 把 `AlignmentTargetContext` 从冻结 decision 无损映射给视觉；正式 strict 开关只在 ROS smoke
   验证旧证据兼容与超时 fail-closed 后启用。
5. start gate 在 field/truth/anchor/manager-IDLE 全部 READY 前调用数为 0，READY 后只成功调用
   一次；随后完成新合并态 90 秒 smoke。
6. 收敛后的航高、航速和路线配置；90 秒通过后才执行 600 秒三槽、返航、落地 Gate。

## 8. 本轮不要求导航组做的事

- 不要求导航实现 YOLO、圆环/红十字几何、视觉 stable ID 的生成、地图投影或复算新相机内参；
  但导航必须在 raw decision、accepted/result/retry 事件中原样携带视觉 stable ID，不能丢失
  后再靠空间邻近反推；
- 不要求删除 tank 资产、权重或 `full` 回归，只要求 r2026 任务准入为 0；
- 不要求在本轮替换 FAST-LIO 或 Fast-Planner；但其高度、可达性和轨迹执行结果仍归导航验收；
- 不要求启动真实 `actuator_pwm`、解锁、实机起飞或真实投递；
- 不接受以视觉放宽新鲜度/地图/关联门槛，或 adapter 跳点来掩盖导航任务层失败。

## 9. 需要双方立即冻结的决定

typed raw decision/result schema、r2026/tank 口径和单一发布者所有权已由本地 clean 核心冻结。
仍需双方冻结：

1. live bridge 的来源、发布者与 planner/target-stage capability；
2. 下一轮导航实际工作高度、速度、航带间距和横偏/yaw frame；
3. 达到何种 `P_confirm/P_interrupt`、确认时间和地图误差后采用对应工作点；
4. baseline 与 `a68925d` 下一轮要调整的参数，以及重新进入 90 s A/B 的判据。

当前接续顺序是：先合入 bridge 并完成 typed 90 秒 smoke，视觉并行在保守工作点邻域补样；
双方前置条件通过后，再做带实际 `P_interrupt` 的 600 秒三投 Gate。

## 10. 2026-08-30 量化工作包络输入与接续顺序

视觉侧最新正式 seed=11 结果为 20/23，但长会话处理 P95=816.9 ms、地图误差 P95=0.602 m，
高速 panzer 同条件一败两胜；因此本轮不把 20/23 作为视觉 Gate PASS，也不立即补满 125 格
或扩 30 seed。完整结果与序贯设计见
[导航算法要求合理性评估与 V-SIM-04 序贯计划](./导航算法要求合理性评估与V-SIM-04序贯计划_20260830.md)。

为了让视觉实验直接服务导航决策，请导航组在下一次接口/配置交付中冻结：

1. 实际搜索航高、航速、最大转弯角速度、航带间距和固定评测路线；
2. 横偏正方向及归一化口径、yaw 参考 frame、实际速度的平均窗口；
3. 导航采用一个工作点所需的 `P_confirm/P_interrupt`、确认时间、地图误差和总漏失率阈值；
4. approach capture 半径；地图误差目标原则上应不大于该半径的一半；
5. manager 哪些状态可被新鲜候选中断，以及 accepted/result/retry 的 stable ID 事件合同；
6. seed 实际改变的布局、入画相位或运动因素，避免把相同条件重复运行误称为多 seed。

双方接续顺序更新为：视觉先收敛长会话吞吐/partial fusion/动态地图误差，再补静态 25 格和
每类 6 点的稀疏速度筛选；导航并行补结果语义、持久队列并收敛安全 A/B。只有双方前置条件
都通过，才进入带实际 `P_interrupt` 的联合 smoke 和 600 s Gate。`P_interrupt` 继续拆分为
`selected|confirmed`、`accepted|valid selected and interruptible SEARCH`、
`actual interrupt|accepted`，导航处于投递、返航或其他不可中断状态不能计作视觉失败。

本轮不要求导航组复算新相机内参。用户已交付 `vision_ws/calibration.yaml`，视觉侧直接将其
作为当前权威内参接入 CameraInfo，不再等待原图或采集参数，也不做独立标定正确性复核。
导航侧只需冻结分辨率、frame/时间戳和 TF 消费合同，并配合安装外参后的联调。

## 11. 2026-08-30 导航下一次交付的可验收清单

第 2 节的 13/23 和第 10 节的 20/23 是两个历史阶段快照。最新受控复跑为
formal 22/23（`MEASURED/NOT_GATED`）、static diagnostic 24/25 和 sparse-speed diagnostic
25/30；后两者是诊断子集，三者的 `P_interrupt` 都为 `null`。相关日志：

```text
logs/vsim04_formal23_latest_seed11_20260830_220302/
logs/vsim04_diag_static25_seed11_20260830_220647/
logs/vsim04_diag_sparse30_retry3_seed11_20260830_223136/
```

这些证据可以供导航选取下一轮工作点，但不能代替导航接受事件，也不能当成
`P_interrupt` 或比赛漏失概率。在导航冻结采用门槛、重复数和实际工作点前，不固定最终
性能分支 hash。

下列五项是 2026-08-30 的验收合同；本地 clean 核心 `a65a616` 已实现并通过纯测试，当前不再
作为“尚未编码”清单，但仍须由 live bridge 和联合运行逐项证明：

1. **accepted**：带 event sequence、stable ID、class、profile、前后状态和 source stamp 的
   `candidate_accepted/approach_started`，并通过带 ID 的 raw decision 显式绑定目标；
2. **result**：区分 `approach_unreachable/capture_timeout/alignment_timeout/release_rejected/
   delivery_succeeded`，携带 reason、retryable、attempt 和 payload slot；
3. **retry**：发布 `retry_scheduled/retry_exhausted`，冻结最大尝试数和冷却时间，成功或
   不可重试失败才进入 terminal，不得在初次 accepted 时永久去重；
4. **persistent queue**：以 stable ID 为键维护 `pending/cooldown/executing/terminal`，区分
   新鲜候选立即中断和长期地图候选延后调度，队列快照可记录可重放；
5. **time scheduling**：记录 mission start/deadline/remaining time，冻结第三投优先、停止覆盖、
   返航余量和超时终止条件，不再由 adapter 隐式猜测。

导航来源继续分开写：外部远程 `main` 仍为 `a68925d` 且只是地图实验；历史运行 manager
为 `5144aa8`；本地 clean 核心为 `a65a616 + 022b763`，由 `7dd2c49` 导入。五项策略已有
纯测试，但 V-CL-06 A/B 仍 FAIL、`a68925d` 不推广、live 600 s 仍未启动。

`r2026` 下继续保留 tank 资产、赛制权重、`full` 回归和 raw 诊断；refiner 不让
tank 抢占蓝环关联，并将其标记为 profile 禁用，正式 selected/accepted 必须为 0。
`logs/vsim04_diag_pillbox_h3p6_640_profilefix_seed11_20260830_225629/` 仍为
`raw_classifier`、`P_confirm=P_selected=0`，因此默认 640 不变，导航不应把该补丁解读为
3.6 m pillbox 召回已修复。

## 12. 2026-08-31 视觉侧完成项与导航侧当前请求

视觉侧已完成 camera-only 10 min 和六点边界重复，导航可以据此先冻结工作点：

- camera-only PASS：`logs/vsim04_soak600b_seed11_20260831_011055/`，wall 600.024 s、
  source 564.863 s、输入/完整 mapped 15.019/13.336 FPS、六产物完整、errors=[]；
- 固定 seed 三次重复：`logs/vsim04_repeat_aggregate_boundary6-seed11-r3-final-307ac5c4/`。
  bridge/panzer 低空 2 m/s 均 0/3，pillbox 低空 2 m/s 2/3；pillbox 3.6 m 的
  0.5/2.0 m/s 分别 1/3、0/3，静态 3.6 m 为 0/3 `raw_classifier`。

由此更新第 9 节第 4 项：请导航组不要把 2 m/s 或 3.6 m 作为当前通用默认；先给出更保守
的航高、航速、航带间距和 approach capture 半径，再由视觉在其邻域补真正多 seed 与横偏。
imgsz=640、现有 profile/关联/新鲜度门槛保持，不用 1280 或放宽准入掩盖边界失败。

camera-only PASS 不改变导航交付门槛。它是固定五目标、不含 tank 的 Gazebo 合成 D435i +
笔记本 Ultralytics，不启动导航/控制/PX4/执行机构，`P_interrupt=null`；run 名含 seed11，
但 soak manifest 无 seed 字段且场景不是随机场。tank/disallowed/stale selected=0 只说明该
路线未错误选择，不能代替导航的 accepted=0 审计。V-CL-06 A/B 仍 FAIL，导航三投 600 秒
仍未启动。

截至 2026-08-31 的来源冻结如下：

- 导航外部仓仍仅 `main@a68925d15293e5510e2b4351c6b3d9bc5aa136ab`，无 branch/tag；
- 正式 manager 源码仍为 `5144aa8f536bdcd214aea2f39ada558383b3bcb0`；
- 联动分支 remote/current `8255aa4` 已是当前性能分支 HEAD 的祖先，不代表导航生命周期更新。

上述任务策略已由本地 clean 核心实现并有确定性测试；联合 Gate 的前置现改为：审核并合入
live execution bridge，产生真实 planner/target-stage typed result，完成新合并态 90 秒 smoke。
通过后才启动带真实 `P_interrupt` 的 600 秒三投。视觉 adapter 不复制这些策略。

## 13. 2026-08-31 当前请求收口

当前合并态为 `db80dfd6d22f31ab682d02ebb39a52a1e082384e`。外部导航远程仍为
`a68925d15293e5510e2b4351c6b3d9bc5aa136ab`；本地 clean manager/runtime 来源是
`a65a616f209bfc7dd4d788ebb36609589cea5418`，planner telemetry 来源是
`022b7636b1661304ce2d47e9368c392adc67997d`，导入基线为 `7dd2c49`。

已完成且纯测通过：持久候选队列、有限重试、三槽、510/600 s、typed contract；一次性
random-field start gate；视觉 `AlignmentTargetContext/ReleaseEvidenceContext`；V-SIM typed
decision/result evaluator。旧消息 MD5 保持，新增 strict/start/typed 功能默认关闭，不改变
旧链运行图。

唯一 P0 集成缺口是 live execution bridge。桥接完成前没有真实 `/fastplanner/goal`，没有
planner/target-stage `STARTED`，`P_interrupt` 必须保持 `null`，新合并态 90 秒和 600 秒 Gate
都不能标 PASS。下一交付应是“bridge + 单发布者静态断言 + 90 秒 typed smoke”，不是再写一套
队列/重试策略。

相机状态更新：用户已交付 `vision_ws/calibration.yaml`，视觉组已将其作为当前权威内参接入
1280×720 ROS CameraInfo profile；不再要求原始图片、标定板规格或采集设置，也不做独立标定
正确性复核。实际相机输出、Image/CameraInfo frame 与时间戳一致性、安装外参、ROS TF、
OrangePi 和实拍定量真值仍分别保留为开放项，不能因 YAML 已接入而标为整机通过。

## 14. 2026-08-31 bridge 到位后的导航侧当前请求

第 13 节“唯一缺口是 live execution bridge”已由后续实现关闭。导航 PR #6 Draft HEAD
`3864a7c` 的四个 bridge 提交已完整等价导入；视觉集成只把既有末端上下文和结果接入同一
executor，没有新增消息、服务、action、compat adapter 或第二 planner-goal publisher。
正式 ROS 图实跑确认 `/navigation/planner_bridge` 是 `/fastplanner/goal` 唯一发布者。

当前请求只剩一个 P0：请导航/建图侧恢复仿真 `/freedom/static_pointcloud`。首轮硬 Gate
`logs/vcl06_typed_smoke90_r2_20260831_060512/gate_status.json` 中 field/truth/anchor/contact/
bridge 均 READY，最大高度 2.0753 m、0 碰撞/越界/超高、tank selected/accepted=0；manager
升空后持续以 `map_missing` 拒绝 start，decision/result 均为 0。运行日志同时出现仿真 Livox
PointCloud2 缺 `intensity/time/ring` 与 FAST-LIO 跳过 scan，需由地图链恢复有效时间戳、
`camera_init` frame 和持续新鲜发布。

验收方式保持原合同：不关闭 `readiness/require_map`，不放宽 `map_max_age`，不让视觉伪造
地图；原样重跑 90 秒，至少取得同一 stable ID 的 selected→typed decision→实际
`MissionCommand.APPROACH`→target-stage 事件。历史 baseline/a68925d A/B 仍 FAIL，满足安全
推广条件前不运行 600 秒 Gate。另请把 PR Conversation 从旧 `c12ee0c`/13 commits 更新到
`3864a7c`/16 commits；不要为兼容旧描述继续增加代码开关。
