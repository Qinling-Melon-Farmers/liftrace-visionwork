# 导航组任务链与新视觉联调 HANDOFF

更新时间：2026-08-26

状态：**接口已接通，单轮 headless 完成两次安全投递；600 s 三投 Gate 未通过。**

## 1. 本轮交付边界

导航组成果来自：

- 仓库：`https://github.com/sakelier/liftrace-controlwork.git`
- 基线：`main@5144aa8f536bdcd214aea2f39ada558383b3bcb0`
- 随附说明：根目录 `近期工作说明_2026-08-25.md`（保持原文）

本分支纳入了导航组 Python manager、搜索/候选策略、`sim.yaml`、原 launch 和 Python
package setup。`target_search_manager_py.py`、`search_types.py`、`sim.yaml` 与原仓库一致；
其他策略文件只有文件末尾空白差异，无逻辑修改。未纳入不承担实际任务逻辑的 C++ 占位
节点，也未复制导航仓库中的重复视觉 launch；现有 Phase D 和 TF 入口已提供等价能力。

导航组源码不负责投递许可和旧控制精对准，因此新增的内容全部放在外围：

- `/target_search_manager_py` 保持原状态机和到达判据，目标重映射到
  `/navigation/goal_raw`；
- `/navigation_visual_delivery_adapter` 是集成适配器，负责起飞/地图门控、转发目标、视觉
  捕获、旧控制 ALIGN、安全 mock ACK、恢复以及三投后的返航/降落；
- `/navigation_visual_delivery_adapter` 独占 `/fastplanner/goal`；
- 原临时 `coverage_search_manager` 不启动；
- 仿真只启动 `mock_raw_servo_server`，不启动真实 `actuator_pwm`。

集成 launch：

```text
uav_mission/navigation_search_delivery_toudi4.launch
```

## 2. 当前运行链

```text
导航组 SearchPolicy
  -> target_search_manager_py
  -> /navigation/goal_raw
  -> navigation_visual_delivery_adapter
  -> /fastplanner/goal
  -> Fast-Planner / patrol_control

单下视相机
  -> merged_standard 六分类 + circle/red_cross 几何
  -> refiner -> map projector -> target_memory
  -> /uav_vision/selected_target
  -> 导航组 manager 中断覆盖并抵近
  -> adapter CAPTURE
  -> 旧控制 ALIGN
  -> release_permission -> guarded /Servo -> software mock ACK
  -> adapter 恢复导航组被中断索引
```

导航组原 `sim.yaml` 使用 world 坐标边界。本仓库飞机出生于主 H
`world=(-0.493412,-1.772690)`，集成 launch 只做一次坐标平移，最终 `camera_init` 搜索边界为
`x=[-3.106588,3.093412]`、`y=[-0.227310,7.772690]`；没有修改原配置文件。

## 3. 候选与权重的真实语义

当前逻辑**不是全局记忆目标按权重排队**，而是“当前新鲜候选局部按权重选择 + 发现时序
驱动立即中断”。具体为：

1. `target_memory` 给当前候选按类别权重排序：
   `red_cross=10, tank=5, panzer=2.5, bridge=2, pillbox=1.5, tent=1`；
2. 只有已经确认、最后观测年龄不超过 `0.5 s` 的候选能发布为
   `/uav_vision/selected_target`；若同一帧/时间窗同时可见多个目标，权重高者优先；
3. 导航组 `CandidatePolicy` 只检查 `map_valid/state/class/有限坐标/新 ID`，不读取权重；
4. manager 在 `SEARCH` 收到第一个合法 `selected_target` 后立即切到 `APPROACH`；
5. `APPROACH/TARGET_HOLD` 期间的新目标回调被忽略，不进入持久任务队列；
6. ID 在准入时就加入已接受集合。即使后续 `capture_timeout`，同一 ID 也不会再次执行。

因此本轮 pillbox 与 tent 同区域出现时，pillbox 因 `1.5 > 1.0` 先被选中，证明了局部权重
有效；但尚未看见的 bridge/panzer/red_cross 不会参与比较，失败 pillbox 也没有冷却重试。
若要满足“全场记忆—权重队列—三投”，任务层后续应消费 `/uav_vision/targets` 的长期地图
候选，显式维护队列、执行结果、冷却/重试和剩余时间，而不能只订阅 `selected_target`。

## 4. 2026-08-26 headless 结果

正式分析日志：

```text
logs/navigation_upstream_visual_delivery_headless_model_20260826_023411/
```

模型：

```text
vision_ws/runs/liftrace_6cls_v5_merged_standard_20260714/weights/best.pt
```

关键结果：

| 项目 | 结果 |
| --- | --- |
| 任务终态 | FAIL，`mission_timeout` |
| 仿真任务时间 | 600.0 s |
| 覆盖推进 | 16 点中的索引 0～8 已到达，正在执行索引 9 |
| 正式发现 | pillbox、tent、bridge，共 3 个 stable ID |
| 成功投递 | tent 槽 1、bridge 槽 2；均为 `drop_circle` + guarded ACK |
| 安全拒绝 | pillbox 一次，`capture_timeout`，未盲投 |
| 第三投/返航/降落 | 未进入，未验收 |
| 目标发布 | 16 次，无周期性重复发布 |
| 话题所有权 | manager 独占 raw goal；adapter 独占 planner goal；旧临时 manager 不存在 |
| 平面边界 | 0 次越界 |

随机红十字真值为 world `(0.5723,4.5496)`，换算为本地约 `(1.066,6.322)`；超时时飞机
最大本地 y 仅约 `4.875`，尚未覆盖到该位置，因此本轮不能评价红十字在线召回或投递。
搜索长航段的 ROS 日志还记录到高度短时超过 4 m（约 4.7 m），需要导航/轨迹跟踪侧结合
规划命令和 MAVROS pose 复核；不能用视觉适配器跳点掩盖。

正式运行前的两次诊断不作为 Gate：一次模型路径为空，只能验证导航；一次用固定 45 s
航段总时长误判正在前进的航点。当前适配器已改为“持续无进展才超时”，不重发、不跳点。

## 5. 当前阻塞与建议归属

### P0：导航/任务组

- 在导航组自有新分支设计持久候选队列：允许类目 profile、权重、地图位置、距离、观测
  年龄、已投/失败状态、冷却重试和剩余时间；本分支不修改上游 manager；
- 把“新鲜观测可立即中断”和“已记忆候选可稍后执行”分成两套准入；
- 给 manager 增加明确投递结果接口，失败候选不能永久丢失，也不能无上限重试；
- 优化 10 min 时序：第三投优先于完成全覆盖，三投后立即返航；
- 复核 2.2 m 搜索航点执行时的高度上冲、长航段轨迹和 20×20×5 地图 z 边界。

### P0：视觉组

- 保持 `/uav_vision/targets` 的 stable ID、地图坐标、质量、`last_seen` 和拒绝原因可靠，
  不把长期记忆伪装成当前新鲜观测；
- 为随机红十字落地“搜索阶段疑似线索可抵近、投递前双确认”的分级候选；
- 针对 pillbox `capture_timeout` 保存/回放抵近阶段图像，量化 1.2 m 高度下圆环是否出画、
  关联是否断裂，再决定相机高度或几何阈值，不凭单轮调参；
- 在下一轮报告中增加搜索/接近/捕获/对准分阶段耗时和最大高度，便于定位时间损耗。

### 联合 Gate

- 固定同一 world/seed/模型，600 s 内完成 3 个不同 stable ID 的顺序槽位投递、返航和落地；
- 0 越界、0 碰撞、0 错误释放；搜索状态高度不得超过 4 m；
- 随机红十字只有航线实际覆盖其视场后，才评价“漏检”；
- headless 通过后再启动 GUI/RViz。本轮按用户要求暂缓 GUI。

## 6. 复现命令

```bash
cd /home/xhj/liftrace
export UAV_VISION_MODEL_PATH=/home/xhj/liftrace/vision_ws/runs/liftrace_6cls_v5_merged_standard_20260714/weights/best.pt
SIM_NO_RECORD=1 bash top_level_scripts/sim_run.sh \
  navigation_upstream_visual_delivery_headless \
  roslaunch uav_mission navigation_search_delivery_toudi4.launch \
  gui:=false rviz:=false enable_debug_image:=false \
  spawn_red_cross:=true red_cross_seed:=0
```

注意：当前 `random_red_cross_spawner.py` 把 `seed=0` 解释为系统随机种子，并非可复现的
固定零号种子；正式多 seed Gate 前应由导航/仿真组在独立分支修正种子契约。

## 7. Git 状态与接续规则

- 当前开发分支：`feat/navigation-mission-visual-delivery`；
- 不直接合并或推送 `main`；
- 导航组原仓库/提交和本地原说明文档均已记录；
- 后续若修改导航组源码，必须由导航组在其负责分支完成或先形成跨组接口约定；
- 所有仿真继续走 `sim_run.sh`，真实舵机、解锁和实机起飞仍禁止自动执行。
