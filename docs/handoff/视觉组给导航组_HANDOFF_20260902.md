# 视觉组 → 导航组 HANDOFF

更新时间：2026-09-02
适用范围：V-SIM-04 A/B/C/D、OrangePi 视觉链、V-CL-06 联合上下文
首读原则：本文件是移交包唯一入口；逐项数值以包内 `evidence_index.csv` 和对应 run 的
`summary.json` 为准。

## 1. 一页结论

| 项目 | 已执行 | 结果 | 当前边界 |
| --- | ---: | --- | --- |
| A 静态高度 | 25/25 | confirm/selected=24/25 | 单 seed 诊断，唯一失败为 pillbox@3.6 m |
| B 高度×速度 | 100/100 | confirm/selected=83/100 | 同一 revision 完整 B100；处理 P95 超阈值，DIAGNOSTIC_ONLY |
| C 横向偏差 | 25/25 | 完整入画 confirm=15/15、selected=13/15 | 全可见性分母为 15/25、13/25，NOT_GATED |
| D 视角/运动/多目标 | 16/50 | supported16 confirm/selected=16/16 | 其余 34 项为 NOT_RUN，不是算法失败 |
| OrangePi USB+ROS | 600 s | PASS | 无安装外参，不代表地图投影或整机闭环 |
| OrangePi 实拍全链 | 4622/4622 | PASS | ROS+RKNN+OpenCV 功能审片，无人工真值/同步 pose/外参 |
| V-CL-06 最新本地上下文 | 1 次联合 run | FAIL | 已有一次 APPROACH/CAPTURE/release；超高 2.85 mm，且运行时工作区未冻结 |

最重要的解释边界：视觉侧 `selected` 不能代替导航真实中断。所有 visual-only 运行的
`P_interrupt` 均为 `null`；只有同一完整动作键出现导航接受、APPROACH 和
`STARTED/CAPTURE` 执行证据时，才能记为 interrupt。

## 2. 冻结版本与来源

| 层次 | Revision | 含义 |
| --- | --- | --- |
| 当前集成基线 | `87c2d9f` | 导航执行面合入视觉 C/D 与板端验收后的聚合分支 |
| B100 受测视觉 | `1e48c22` | B100 matrix/slice；本轮 B100 的唯一视觉 revision |
| B100 导航来源标识 | `5144aa8f536bdcd214aea2f39ada558383b3bcb0` | visual-only manifest 冻结的历史 manager 来源，不代表本轮执行导航 |
| C25 受测视觉/导航标识 | `bd278ecf` / `531a12ce` | 零畸变投影修复后的 C25 run |
| D16 受测视觉/导航标识 | `fdc5d818` / `49dd9dc6` | supported16 轨迹关联 run |
| OrangePi 600 s | `abc5103` | 新相机、CameraInfo、统一 RKNN 与 ROS 像素链长稳 |
| OrangePi 全视频 | `59c74b6` | 修复 RKNN 冷启动订阅竞态后的全链 revision |
| 外部导航地图实验 | `a68925d15293e5510e2b4351c6b3d9bc5aa136ab` | 6 树+3 塔实验，不含 manager 生命周期更新 |

模型：`liftrace_6cls_v5_merged_standard_20260714/weights/best.pt`（笔记本仿真）和
`merged_standard_fp32.rknn`（OrangePi）；profile=`r2026`；正式矩阵不含 tank。

## 3. 指标语义

### 3.1 P_confirm

一次 trial 从目标进入其声明的有效观察窗口开始，到离开窗口结束。窗口内同一物理目标必须
产生满足当前准入条件的候选：类别/profile 合法、连续帧满足、地图和关联有效、无拒绝原因、
年龄不超过 0.5 秒。partial trial 使用独立可见性口径，不能混入“完整入画”分母。

### 3.2 P_selected

满足 P_confirm 后，同一 stable ID 必须在离开窗口前出现在
`/uav_vision/selected_target`。有 confirmed 候选但未被当前排序选中，只计 confirm。

### 3.3 P_interrupt

visual-only 一律为 `null`，不得用 selected、typed decision、planner goal 或 planner
arrival 中任何一项替代。联合链必须在同一
`mission_id + decision_seq + target_id + target_first_seen + attempt + payload_slot`
动作键上得到真实执行 `STARTED/CAPTURE` 证据。

### 3.4 状态词

- `MEASURED`：trial 和六件套完整，不等于算法 PASS。
- `DIAGNOSTIC_ONLY`：诊断面；数值再高也不能改写为联合 Gate PASS。
- `NOT_GATED`：测量完成，但采用阈值未全部冻结。
- `NOT_RUN`：当前场景/runner 合同不具备有效测量能力，不计为 0 或失败样本。
- `FUNCTIONAL_REPLAY_ONLY`：实拍功能审片，无人工真值/同步 pose，不报告真实召回或地图误差。

## 4. V-SIM-04 结果

### 4.1 A：静态高度

- Run：`vsim04_diag_static25_seed11_20260830_220647`。
- 条件：五类 × `{1.2,1.8,2.4,3.0,3.6}` m，共 25 项。
- 完成 25/25，P_confirm=P_selected=0.96；唯一失败为
  `static_pillbox_h3p6` 的 raw classifier。
- 单 seed=11，只能表示确定性覆盖，不能称为 96% 真实召回率。

### 4.2 B：完整高度×速度曲面

- Run：`vsim04_diag_b100_seed11_20260902_174132`。
- Revision：vision=`1e48c22`，navigation manifest=`5144aa8...`。
- 条件：五类 × 五高度 × 四速度，共 100 个唯一动态 trial；tank=0。
- 完成 100/100，P_confirm=P_selected=0.83，17 个 trial 首阻断均为
  `target_memory_admission`；`P_interrupt=null`。
- P95 confirmation processing=448.181 ms，超过冻结的 200 ms；P95 map error=0.203479 m，
  通过 0.25 m；最终 `DIAGNOSTIC_ONLY`、`hard_failure=false`、`validation_errors=[]`。
- 类别分层：tent 0.95、pillbox 0.65、bridge 0.80、panzer 0.85、red_cross 0.90。
- 高度分层：1.2 m=0.40、1.8 m=0.95、2.4 m=1.00、3.0 m=1.00、3.6 m=0.80。
- 速度分层：0.5 m/s=0.96、1.0 m/s=0.84、1.5 m/s=0.76、2.0 m/s=0.76。

导航当前可先采用的保守搜索工作域是约 2.4–3.0 m、优先不高于 1.0 m/s；这是单 seed
边际统计推导的暂行参数，不是跨 seed 置信区间。历史 formal23/sparse30 仅作 revision 对照，
不得与本轮 100 格拼接。

### 4.3 C：横向偏差

- Run：`vsim04_c25_seed11_20260902_020233`。
- 五类 × center/left75/right75/left_partial/right_partial，25/25 完成。
- 完整入画分母：P_confirm=15/15，P_selected=13/15。
- 全可见性设计分母：P_confirm=15/25，P_selected=13/25。
- P95 processing=147.554 ms，P95 map error=0.110304 m；`NOT_GATED`。
- partial 不得混入完整入画分母；单 seed 结果不足以冻结最终航带宽度。

### 4.4 D：视角、运动和多目标

- Run：`vsim04_diag_d50_supported_seed11_20260902_021311`。
- 当前合法 supported slice 16/16 完成，P_confirm=P_selected=1.0，P95 processing=104.620 ms，
  P95 map error=0.083298 m，`P_interrupt=null`，`DIAGNOSTIC_ONLY`。
- 这 16 项覆盖 center/quadrant、0/45/90/135°、constant/accel_decel/turn、单目标。

其余 34 项全部保持 `NOT_RUN`：

- 11 个 edge 与 9 个 partial 缺独立 center-in-frame/clipped 观察窗口；
- d12/d16/d32 不满足“完整入画后离画”的有效窗口；
- d39 轨迹超过冻结的 4.8 m 场界；
- multi41–50 缺第二靶/H spawner、独立 truth ID 和逐目标关联，其中 d46–d49 含 H。

当前不存在“能直接跑但忘了跑”的 D 项。绕过 readiness 强行运行不能形成合法样本；D34 的
概率字段必须留空，不能填 0。

## 5. OrangePi 结果

### 5.1 新相机与 600 秒 ROS

- raw/CameraInfo：29.164/29.163 Hz；RKNN perf：13.705 Hz。
- processing P50/P95：62.186/73.478 ms；backend=`rknn_unified`，level 全部 OK。
- 清洁 600 秒内 USB 新错误、禁用节点、视觉节点崩溃均为 0。
- 后 213 秒 NPU 100%@1 GHz，最高温度 69.307°C，未触发 cooling。

该结果验证 USB、CameraInfo、ROS、RKNN/NPU 和像素处理，不包含 FAST-LIO、导航、飞机运动
或安装外参后的地图投影。

### 5.2 `real_target.mp4` 完整 ROS+RKNN+OpenCV 标注视频

实际链路：

```text
ROS 顺序图像发布 / CameraInfo
→ unified RKNN
→ circle/cross/landing OpenCV detector
→ search/landing fusion
→ target_refiner
→ target_map_projector（无 TF 时失败关闭）
→ annotation recorder + perf
```

- 受测 revision：`59c74b6`；先以真正冷启动 10/10 回归首帧竞态。
- 输入/输出：4622/4622 帧；结果：`PASS`；全链墙钟 1364 秒。
- 输出文件：`real_target_rknn_ros_opencv_full_4622.mp4`，大小 843,258,683 B，
  媒体时长 77.053 秒，`nb_frames=4622`。
- 群聊审片副本：`real_target_rknn_ros_opencv_full_4622_h264_review.mp4`，H.264 CRF 22，
  269,418,434 B，仍为 4622 帧/77.053 秒；原始全链 MP4 同时保留。
- perf：4622/4622，backend=`rknn_unified`，frame counter 1→4622；processing
  P50/P95/max=63.795/79.924/131.396 ms，inference=49.622/57.694/102.525 ms，
  总检测数 3467。
- map projector：5135 个 mapped 结果全部 invalid，样本拒绝原因为 `tf_unavailable`，
  map frame=`video_replay_unavailable_map`，符合无外参/TF 的失败关闭合同。
- 禁用节点：监控文件 0 B，收尾复核无 PX4/MAVROS/导航/控制/舵机进程。

该视频暂按用户要求用于人工检查红十字中心、圆环/靶心几何和类别框。它没有人工实例/中心
真值、同步飞机 pose 或安装外参，因此不能报告真实 precision/recall、地图误差或
`P_interrupt`；无外参时 `map_valid=false` 是正确行为。

## 6. V-CL-06 最新协作上下文

包内 `07_VCL06_CONTEXT/latest_gate_status.json` 来自
`vcl06_full_chain_r9_20260902_171808`，仅作当前跨组调试上下文：该 run 启动时导航工作区有
未提交修改，不是可验收冻结证据。

它已经证明：

- field/anchor ready，start gate 只启动一次；
- `/navigation/planner_bridge` 是唯一 planner goal 发布者；
- 记录到 selected=2195、APPROACH=1、CAPTURE started=1、release commit=1、recovery success=1；
- unmatched result=0、碰撞=0、越界=0、tank selected/accepted=0。

这些是计数器进展，不是合格闭环：Gate 同时明确
`committed_targets_were_selected=false`、`real_approach_commands=false`。它仍为 FAIL：
最大高度 4.002849 m，超 4 m 约 2.85 mm；仅记录到 1/3 释放，未完成三次恢复、
走廊门顺序、返航、H 对齐和 LAND。`contract_errors_zero=false` 的直接错误是
`height_limit_violation`。因此下一轮应先在冻结导航 revision 上消除高度越界并完成同一动作键
的三投事务，再跑 600 秒；不要在视觉 adapter 中复制导航队列/重试/调度。

## 7. 接收方使用顺序

```text
HANDOFF.md
evidence_index.csv
02_VSIM04/B_full100_seed11/vsim04/report.md
02_VSIM04/B_full100_seed11/vsim04/summary.json
02_VSIM04/B_full100_seed11/vsim04/vision_search_performance.csv
02_VSIM04/D_supported16_seed11/vsim04/report.md
03_BOARD_EVIDENCE/full_ros_video/summary.json
07_VCL06_CONTEXT/latest_gate_status.json
```

两份 MP4 都不放入结构化 ZIP；群聊优先发送 H.264 审片副本，必要时再发送原始全链文件。
ZIP 内的 summary、perf、ffprobe、预览帧和 `VIDEO_TRANSFER_README.md` 记录对应关系。

## 8. 下一步联合工作

1. 导航采用 B100 的暂行保守工作域，先完成同一 stable ID 的
   selected→APPROACH→CAPTURE 90 秒 smoke。
2. 冻结导航实现后消除 4 m 边界超限，完成三投、三次恢复、走廊门、返航和 LAND。
3. 90 秒目标事务通过后再跑 600 秒联合 Gate；失败保持 FAIL，不在视觉侧复制导航策略。
4. D 后续先实现 edge/partial 观察窗口，再接第二靶/H 与 multi10。
5. 2026-09-05 已取得机械平移：雷达 IMU 到相机光心正下方 `0.20 m`，飞控盒/当前
   `body` 到相机正下方 `0.16 m`。视觉板端入口已按现有 FAST-LIO 树配置
   `body→optical z=-0.16 m`；导航侧需核清仿真 SDF 的 `body`/雷达 IMU 原点，并共同完成
   `optical→body→camera_init` 唯一 TF 发布者和有效地图投影 Gate，不新增接口。

## 9. 禁止扩大解释

- 单 seed B100 不是概率曲面；
- visual-only selected 不是 interrupt；
- D34 NOT_RUN 不是 34 个算法失败；
- 板端实拍视频不是人工真值评测；
- camera-only 600 秒 PASS 不是 V-CL-06 PASS；
- 最新导航 r9 是 dirty-worktree 调试上下文，不是冻结 Gate；
- feature 分支能力不能写成 `main` 已验收能力。
