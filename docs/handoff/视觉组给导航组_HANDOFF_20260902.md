# 视觉组 → 导航组 HANDOFF

更新时间：2026-09-04
适用范围：V-SIM-04 A/B/C/D、OrangePi 视觉链、V-CL-06 联合上下文
首读原则：本文件是移交包唯一入口；逐项数值以包内 `evidence_index.csv` 和对应 run 的
`summary.json` 为准。

## 1. 一页结论

| 项目 | 已执行 | 结果 | 当前边界 |
| --- | ---: | --- | --- |
| A 静态高度 | 25/25 | confirm/selected=23/25 | KS2A543 单 seed 诊断；pillbox@3.0/3.6 m raw classifier 失败 |
| B 高度×速度 | 100/100 | confirm=87/100、selected=86/100 | KS2A543 同 revision 完整 B100；pipeline P95=182.0 ms，DIAGNOSTIC_ONLY |
| C 横向偏差 | 25/25 | 完整入画 confirm=15/15、selected=12/15 | 全设计可见性分母为 15/25、12/25，MEASURED/NOT_GATED |
| D 视角/运动/多目标 | 11/50 | supported11 confirm/selected=11/11 | KS2A543 单目标诊断完成；其余 39 项为 NOT_RUN |
| OrangePi USB+ROS | 600 s | PASS | 无安装外参，不代表地图投影或整机闭环 |
| OrangePi 实拍全链 | 4622/4622 | PASS | ROS+RKNN+OpenCV 功能审片，无人工真值/同步 pose/外参 |
| `liftrace-sim` 本机候选 | B100 + 120/36 点扫描 | 完成 | `ee5d2bd`；C/D 仅归档，远端 push 因权限 403 未完成 |
| V-CL-06 最新本地上下文 | r11 正式联合 run | 三投子链 PASS；整场 FAIL | panzer/bridge/red_cross 三投与恢复 3/3、Wall_15 已通过；阻塞在走廊 route 2 的近目标终态 |

最重要的解释边界：视觉侧 `selected` 不能代替导航真实中断。所有 visual-only 运行的
`P_interrupt` 均为 `null`；只有同一完整动作键出现导航接受、APPROACH 和
`STARTED/CAPTURE` 执行证据时，才能记为 interrupt。

## 2. 冻结版本与来源

| 层次 | Revision | 含义 |
| --- | --- | --- |
| A25/B100/C25 受测视觉 | `ede886fce3a24659bb7fb741207e63c082ac0d5c` | 同一 KS2A543、模型、评测时戳合同下的 A/B/C revision |
| D11 受测视觉 | `12569670ede592e4642c2f0b978e4106e0921f4e` | 在上述基线之上仅修复 D 圆弧测量并异步驱动姿态 |
| A/B/C/D 导航来源标识 | `5144aa8f536bdcd214aea2f39ada558383b3bcb0` | visual-only manifest 的来源标识，不代表本轮执行了导航 |
| OrangePi 600 s | `abc5103` | 新相机、CameraInfo、统一 RKNN 与 ROS 像素链长稳 |
| OrangePi 全视频 | `59c74b6` | 修复 RKNN 冷启动订阅竞态后的全链 revision |
| 导航轻量策略仓 | `ee5d2bd0d023f174c741f8d93bf91d15595db9a9` | 本机导入当前 B100/C25/D11 并完成候选扫描；非远端 main |
| 外部导航地图实验 | `a68925d15293e5510e2b4351c6b3d9bc5aa136ab` | 6 树+3 塔实验，不含 manager 生命周期更新 |
| V-CL-06 r11 导航代码 | `919e438` | r11 实际运行代码；修复目标代际、限高、APPROACH 交接和 ROS 传输序号合同 |
| V-CL-06 导航文档 tip | `c1be1d0` | 上游同名 feature 的 clean tip；只比 r11 代码多本次实跑/HANDOFF 记录 |
| V-CL-06 r11 视觉 overlay | `34ce0c3` | r11 实际使用的冻结视觉实现 |
| 下一轮视觉候选 | `a1bfca9` | 当前视觉分支；关键任务节点/typed 消息与 `34ce0c3` 同 blob，仅增加 raw 像素去畸变投影修正 |

模型：`liftrace_6cls_v5_merged_standard_20260714/weights/best.pt`（笔记本仿真）和
`merged_standard_fp32.rknn`（OrangePi）；profile=`r2026`；正式矩阵不含 tank。

## 3. 指标语义

### 3.1 P_confirm

一次 trial 从目标进入其声明的有效观察窗口开始，到离开窗口结束。窗口内同一物理目标必须
产生满足当前准入条件的候选：类别/profile 合法、连续帧满足、地图和关联有效、无拒绝原因、
年龄不超过 0.5 秒。partial trial 使用独立可见性口径，不能混入“完整入画”分母。

### 3.2 P_selected

满足 P_confirm 后，同一 stable ID 必须在离开窗口前出现在
`/uav_vision/selected_target`。有 confirmed 候选但未被当前排序选中，只计 confirm。

### 3.3 P_interrupt

visual-only 一律为 `null`，不得用 selected、typed decision、planner goal 或 planner
arrival 中任何一项替代。联合链必须在同一
`mission_id + decision_seq + target_id + target_first_seen + attempt + payload_slot`
动作键上得到真实执行 `STARTED/CAPTURE` 证据。

### 3.4 状态词

- `MEASURED`：trial 和六件套完整，不等于算法 PASS。
- `DIAGNOSTIC_ONLY`：诊断面；数值再高也不能改写为联合 Gate PASS。
- `NOT_GATED`：测量完成，但采用阈值未全部冻结。
- `NOT_RUN`：当前场景/runner 合同不具备有效测量能力，不计为 0 或失败样本。
- `FUNCTIONAL_REPLAY_ONLY`：实拍功能审片，无人工真值/同步 pose，不报告真实召回或地图误差。

## 4. V-SIM-04 结果

### 4.1 A：静态高度

- Run：`vsim04_diag_static25_seed11_20260904_140501`。
- 条件：五类 × `{1.2,1.8,2.4,3.0,3.6}` m，共 25 项。
- 完成 25/25，P_confirm=P_selected=0.92；失败为
  `static_pillbox_h3p0/h3p6` 的 raw classifier。
- 同源 pipeline/detector processing/map P95=`164.813 ms/97.783 ms/0.203769 m`，TF failure=0。
- 单 seed=11，只能表示确定性覆盖，不能称为 92% 真实召回率。

### 4.2 B：完整高度×速度曲面

- Run：`vsim04_diag_b100_seed11_20260904_140800`。
- Revision：vision=`ede886f`，navigation manifest=`5144aa8...`。
- 条件：五类 × 五高度 × 四速度，共 100 个唯一动态 trial；tank=0。
- 完成 100/100，P_confirm=0.87、P_selected=0.86，`P_interrupt=null`。
- 13 个未确认 trial 的首阻断为 `raw_classifier=7`、`geometry_association=2`、
  `target_memory_admission=3`、`truth_visibility=1`；另有 tent@1.2 m/2.0 m/s 已确认但未选中。
- 合同指标 P95：same-host pipeline=`181.990 ms`、detector processing=`145.724 ms`、
  map error=`0.203484 m`，TF failure=0；最终 `DIAGNOSTIC_ONLY`、硬失败/终态错误均为 0。
- 旧 `448.181 ms` 是 D435i 历史运行中 recorder 两个独立订阅回调的传输诊断值，不再作为确认链合同指标。
- 类别 confirm/selected：tent 0.95/0.90、pillbox 0.50/0.50、bridge 1.00/1.00、
  panzer 0.90/0.90、red_cross 1.00/1.00。
- 高度 confirm/selected：1.2 m=0.85/0.80、1.8 m=0.95/0.95、2.4 m=0.95/0.95、
  3.0 m=0.80/0.80、3.6 m=0.80/0.80。
- 速度 confirm/selected：0.5=0.92/0.92、1.0=0.92/0.92、1.5=0.84/0.84、
  2.0 m/s=0.80/0.76。

导航当前可先采用的保守搜索工作点是约 2.4 m、优先不高于 1.0 m/s；本轮该子集五类 10/10
均 selected。3.0/3.6 m 的 pillbox 全部失败，不应继续作为跨类别通用高度。这是单 seed
边际统计推导的暂行参数，不是跨 seed 置信区间。历史 formal23/sparse30 仅作 revision 对照，
不得与本轮 100 格拼接。

`liftrace-sim` 使用该 B100 完成 120 点搜索曲面后，基线保留 2.4 m/1.0 m/s；36 点动态阈值
扫描得到 `p=0.70, gamma=0.50`，但仍登记为含假设任务模型的未启用候选。C25/D11 未硬接进
Cue 或运行时接口，避免把评测维度扩展成新的跨组合同。

### 4.3 C：横向偏差

- Run：`vsim04_c25_seed11_20260904_135946`，vision=`ede886f`。
- 五类 × center/left75/right75/left_partial/right_partial，25/25 完成。
- 完整入画分母：P_confirm=15/15，P_selected=12/15；center/left75/right75 的 selected
  分别为 4/5、5/5、3/5。
- 全设计可见性分母：P_confirm=15/25，P_selected=12/25；10 个 partial 不进入正式完整入画分母。
- P95 pipeline/detector processing/map=`158.690 ms/96.467 ms/0.216720 m`，TF failure=0；
  `MEASURED/NOT_GATED`。
- partial 不得混入完整入画分母；单 seed 结果不足以冻结最终航带宽度。

### 4.4 D：视角、运动和多目标

- Run：`vsim04_diag_d50_supported_seed11_20260904_144351`，vision=`1256967`。
- 当前 KS2A543 合法 supported slice 11/11 完成，P_confirm=P_selected=1.0；运动/横向样本
  `792/811`，实际角速度 P95=`0.6136 rad/s`，规划轨迹横向误差 P95=0。
- P95 pipeline/detector processing/map=`265.232 ms/72.576 ms/0.152282 m`，TF failure=0，
  终态错误/硬失败为 0，`P_interrupt=null`，`DIAGNOSTIC_ONLY`。
- pipeline 超过 200 ms 参考线，D 的采用阈值又尚未冻结，因此只能说明这 11 个可测单目标工况
  全部找到并选中，不能宣称 D Gate PASS。

其余 39 项全部保持 `NOT_RUN`：

- 20 个 edge/partial 缺独立 center-in-frame/clipped 观察窗口；
- 7 个 center/quadrant 不满足“完整入画后离画”的有效窗口；
- 2 个轨迹超过冻结的 4.8 m 场界；
- multi41–50 缺第二靶/H spawner、独立 truth ID 和逐目标关联，其中 d46–d49 含 H。

当前不存在“能直接跑但忘了跑”的 D 项。绕过 readiness 强行运行不能形成合法样本；D39 的
概率字段必须留空，不能填 0。

## 5. OrangePi 结果

### 5.1 新相机与 600 秒 ROS

- raw/CameraInfo：29.164/29.163 Hz；RKNN perf：13.705 Hz。
- processing P50/P95：62.186/73.478 ms；backend=`rknn_unified`，level 全部 OK。
- 清洁 600 秒内 USB 新错误、禁用节点、视觉节点崩溃均为 0。
- 后 213 秒 NPU 100%@1 GHz，最高温度 69.307°C，未触发 cooling。

该结果验证 USB、CameraInfo、ROS、RKNN/NPU 和像素处理，不包含 FAST-LIO、导航、飞机运动
或安装外参后的地图投影。

### 5.2 `real_target.mp4` 完整 ROS+RKNN+OpenCV 标注视频

实际链路：

```text
ROS 顺序图像发布 / CameraInfo
→ unified RKNN
→ circle/cross/landing OpenCV detector
→ search/landing fusion
→ target_refiner
→ target_map_projector（无 TF 时失败关闭）
→ annotation recorder + perf
```

- 受测 revision：`59c74b6`；先以真正冷启动 10/10 回归首帧竞态。
- 输入/输出：4622/4622 帧；结果：`PASS`；全链墙钟 1364 秒。
- 输出文件：`real_target_rknn_ros_opencv_full_4622.mp4`，大小 843,258,683 B，
  媒体时长 77.053 秒，`nb_frames=4622`。
- 群聊审片副本：`real_target_rknn_ros_opencv_full_4622_h264_review.mp4`，H.264 CRF 22，
  269,418,434 B，仍为 4622 帧/77.053 秒；原始全链 MP4 同时保留。
- perf：4622/4622，backend=`rknn_unified`，frame counter 1→4622；processing
  P50/P95/max=63.795/79.924/131.396 ms，inference=49.622/57.694/102.525 ms，
  总检测数 3467。
- map projector：5135 个 mapped 结果全部 invalid，样本拒绝原因为 `tf_unavailable`，
  map frame=`video_replay_unavailable_map`，符合无外参/TF 的失败关闭合同。
- 禁用节点：监控文件 0 B，收尾复核无 PX4/MAVROS/导航/控制/舵机进程。

该视频暂按用户要求用于人工检查红十字中心、圆环/靶心几何和类别框。它没有人工实例/中心
真值、同步飞机 pose 或安装外参，因此不能报告真实 precision/recall、地图误差或
`P_interrupt`；无外参时 `map_valid=false` 是正确行为。

## 6. V-CL-06 最新协作上下文

包内 `07_VCL06_CONTEXT/latest_gate_status.json` 来自
`vcl06_p0_seed11_r11_20260904_030218`。该轮使用导航代码 `919e438`、视觉 overlay
`34ce0c3`、seed=11、profile=`r2026` 和 mock Servo；没有执行真实舵机或实机运动。

它已经证明：

- field/anchor ready，start gate 只启动一次；
- `/navigation/planner_bridge` 是唯一 planner goal 发布者；
- panzer/slot 1、bridge/slot 2、red_cross/slot 3 均完成
  selected→APPROACH→CAPTURE→release commit→recovery，三项计数均为 3/3；
- `visual_delivery_audit.json=PASS`，unmatched result=0，tank selected/accepted=0；
- 最大高度 `3.147812 m`，碰撞、越界和高度违规均为 0；
- 三投后已进入 11 点路线、完成第 1 点并有效通过 Wall_15。

整场 Gate 仍为 FAIL。decision 13 对应 route index 2；Fast-Planner 已把请求点调整为 effective
goal，机体进入约 0.13 m 后，规划器的 `<0.2 m` `Close goal` 分支拒绝生成短轨迹，而执行桥仍等待
`TRAJECTORY_FINISHED`，最终第 21 个显式失败事件触发 `planner_attempt_limit_exceeded`，manager 以
`return_home_failed` 中止。该问题属于导航规划生命周期，不是视觉候选、stable ID、类别排序或
投递事务失败；不能靠增加重试次数、全局放宽航点容差或在视觉 adapter 中复制导航策略解决。

当前视觉 `a1bfca9` 已通过联合 launch 的静态解析：`uav_vision` 确实解析到当前视觉工作树，
`target_detector/target_memory/drop_aligner/navigation_planner_bridge` 均存在；关键运行节点和两份上下文
消息与 r11 的 `34ce0c3` 逐 blob 一致。因此后续可用当前视觉分支替代旧 overlay。不过 r11 使用的
仿真相机仍是 `640×480`、HFOV=`1.047 rad`，不是新 KS2A543 的 `1280×720`、约 `82.85°`；r11
证明的是任务逻辑闭环进展，不是新实机相机几何下的整机 Gate。

还有一个复现边界：r11 的顶层 `manifest.yaml` 记录了启动所在旧根工作区 `93d5c5c+dirty`，且
`launch_cmd` 仍写 r10 脚本名，没有机器可读地冻结实际导航/视觉 revision。Gate 与 run.log 的功能
结果可信，但该 manifest 不能作为最终合并证据；下一轮必须显式记录两个仓 revision、相机模型和
实际 launch 参数。

## 7. 接收方使用顺序

```text
HANDOFF.md
evidence_index.csv
02_VSIM04/B_full100_seed11/vsim04/report.md
02_VSIM04/B_full100_seed11/vsim04/summary.json
02_VSIM04/B_full100_seed11/vsim04/vision_search_performance.csv
02_VSIM04/D_supported11_seed11/vsim04/report.md
03_BOARD_EVIDENCE/full_ros_video/summary.json
07_VCL06_CONTEXT/latest_gate_status.json
```

两份 MP4 都不放入结构化 ZIP；群聊优先发送 H.264 审片副本，必要时再发送原始全链文件。
ZIP 内的 summary、perf、ffprobe、预览帧和 `VIDEO_TRANSFER_README.md` 记录对应关系。

## 8. 下一步联合工作

1. 导航在单一 planner bridge 内补有界近目标终态：仅对已 ACCEPTED、已有有效轨迹生命周期、
   effective goal 范围内且低速持续驻留的当前决策完成；不得把远距离无路或门前停住误判为成功。
2. 先保持 r11 的 seed=11、2.2 m 搜索高度和旧仿真相机不变复跑 r12，以隔离该修复；要求继续通过
   Wall_20、Wall_22、H 对齐、AUTO.LAND、落地与解锁，只有 `gate_status.json=PASS` 才算整场通过。
3. 首次整场 PASS 后，再独立切换 KS2A543 相机几何，并对 2.2 m 与视觉 B100 候选 2.4 m/1.0 m/s
   做配对 A/B；不要与近目标修复同轮改变。
4. D 后续先实现 edge/partial 观察窗口，再接第二靶/H 与 multi10；D11 的 265 ms pipeline
   需在整机负载下复核，但不阻塞导航当前近目标修复。
5. 相机安装外参完成后补 `optical→body→camera_init` TF 和板端有效地图投影。

## 9. 禁止扩大解释

- 单 seed B100 不是概率曲面；
- visual-only selected 不是 interrupt；
- D39 NOT_RUN 不是 39 个算法失败；
- 板端实拍视频不是人工真值评测；
- camera-only 600 秒 PASS 不是 V-CL-06 PASS；
- r11 三投子链 PASS 不是整场 V-CL-06 PASS；
- r11 顶层 manifest 未冻结实际双仓 revision，不能单独作为最终合并证据；
- r11 的旧 60° 仿真相机结果不是 KS2A543 新相机几何验收；
- feature 分支能力不能写成 `main` 已验收能力。
